# Phase 0: Bootstrap & Spikes - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-06
**Phase:** 0-Bootstrap & Spikes
**Mode:** `--auto` (Claude auto-selected recommended option for every gray area; no AskUserQuestion calls)
**Areas discussed:** Spike 격리 위치, 부분 통과 정책, services.yaml 작성 방식, state/ 위치, .env 검증 시점, Spike 5 범위, Spike 3 범위, Test framework

---

## Spike 격리 위치

| Option | Description | Selected |
|--------|-------------|----------|
| `spikes/` 임시 디렉토리에 격리 | 메인 `src/`, `state/`와 분리. Spike별 독립 실행 스크립트. green pass 후 정리. | ✓ |
| 메인 src/ 안에 자연스럽게 통합 | spike 코드가 곧 production 코드의 첫 모듈이 됨. | |
| GitHub gist / 외부 sandbox | 메인 repo 무관. | |

**Auto choice rationale:** 메인 코드 구조를 spike가 오염시키지 않음. 각 spike가 독립 검증 단위라는 원칙 유지. `state/`는 services.yaml 산출물 전용으로 깨끗하게 시작.

---

## Spike 부분 통과 정책

| Option | Description | Selected |
|--------|-------------|----------|
| No partial pass — 5/5 green 강제 | 한 spike라도 red면 fallback path 강제. Phase 1 진행 차단. | ✓ |
| 4/5 green이면 진행 가능 | 가장 위험도 낮은 spike 1개는 Phase 1과 병행 검증. | |
| Spike별 weight를 둬서 critical만 강제 | Spike 1, 2가 critical이고 나머지는 best-effort. | |

**Auto choice rationale:** Phase 0의 존재 이유는 "Phase 1에서 발견될 risk를 사전에 처리"하는 것. 부분 통과로 진행하면 Phase 1 시간 예산이 폭발한다.

---

## services.yaml 초안 작성 방식

| Option | Description | Selected |
|--------|-------------|----------|
| AI 자동 생성 + 인간 review/edit | `docker ps --format json` → AI 파싱 → 초안. 사용자가 의존성/볼륨 보강. | ✓ |
| 100% 인간 손작업 | 5개 stack 직접 작성. 가장 정확하지만 시간 많음. | |
| AI 자동 생성, review 없이 commit | 초안을 그대로 첫 commit. Phase 1에서 실제 사용 중에 보완. | |

**Auto choice rationale:** 10개 stack 전체 손작업은 2-4h 소요 → Phase 1 시간 예산 폭발 (이게 Phase 0이 신설된 이유). AI 초안 + 인간 review가 시간/품질 trade-off의 sweet spot.

---

## state/ 디렉토리 위치

| Option | Description | Selected |
|--------|-------------|----------|
| 메인 repo 서브디렉토리 | `gons-works/state/` 안에 일반 디렉토리. 메인 repo와 git 통합. | ✓ |
| 별도 submodule | `gons-works/state/`는 별 git repo의 submodule. | |
| 외부 별 repo (homelab-state) | `~/homelab-state/` 별도 repo, 본 repo와 분리. | |

**Auto choice rationale:** PROJECT.md Key Decisions에 이미 lock됨. 첫 milestone 단순화. 추후 분리 옵션은 v2 STATE-SUB-01에 보존.

---

## .env 검증 시점

| Option | Description | Selected |
|--------|-------------|----------|
| Phase 0에서 schema + startup guard 둘 다 작성 | BOOT-04/BOOT-05 요구사항. 첫 코드 실행부터 안전. | ✓ |
| Phase 0에서 .env 작성, 검증은 Phase 1 | Phase 0은 키 채우기까지만. | |
| Phase 1까지 미루기 | spike 단계에서는 hardcode로 진행. | |

**Auto choice rationale:** Phase 1 첫 코드 실행에서 `.env` 누락 발견하면 디버깅 시간 낭비. Phase 0 boundary에 명확히 부합 (BOOT-04/BOOT-05).

---

## Spike 5 (Zod schema) 검증 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 1개 대표 스키마 검증 | `listContainers` 1개로 `z.toJSONSchema()` 동작 확인. | ✓ |
| 5개 tool 스키마 모두 미리 정의 | Phase 1 LOOP 작업의 사전준비를 Phase 0에서. | |
| Schema 자체는 Phase 1, Spike에서는 z.toJSONSchema() API 호출만 검증 | 가장 가벼운 spike. | |

**Auto choice rationale:** Spike의 목적은 "기술 가정 검증"이지 "Phase 1 코드 미리 작성"이 아니다. Phase 0 시간 폭발 방지.

---

## Spike 3 (SSE) 검증 범위

| Option | Description | Selected |
|--------|-------------|----------|
| 최소 Hono 서버 + 1개 더미 SSE endpoint | `/sse-test` 1개 라우터 + 더미 chunk 5개. 브라우저 수동 확인. | ✓ |
| 실제 agent loop 연결한 end-to-end | Claude API와 함께 검증. Phase 1 코드 일부 미리 작성. | |
| Node fetch 단위 검증만 | 브라우저 없이 fetch로 raw event stream만 확인. | |

**Auto choice rationale:** "wire가 동작하는가"가 spike 목적. agent loop를 미리 만들면 Phase 0 boundary 초과.

---

## Test framework

| Option | Description | Selected |
|--------|-------------|----------|
| bun:test (built-in) | STACK.md lock. 별도 설치 불필요. Jest-compatible API. | ✓ |
| Vitest | 광범위 ecosystem, snapshot test 강력. 설치 필요. | |
| Test 없이 manual 실행만 | Spike는 사실 manual smoke test에 가까움. | |

**Auto choice rationale:** STACK.md에 이미 `bun:test` lock. Vitest/Jest 추가는 dependency 증가 + 16h 예산 위반.

---

## Claude's Discretion

다음 사항은 plan-phase에서 Claude가 결정:

- spike별 정확한 파일 분할 (1 spike = 1 file vs 1 spike = 1 디렉토리)
- spike 실행 순서 (ROADMAP.md 우선순위 1→5 그대로 vs 의존성 별로 재정렬)
- `services.yaml` 초안 생성 스크립트의 정확한 출력 형태 (services.yaml schema 자체는 Phase 1 KB-01)
- `.env.example` 작성 여부

---

## Deferred Ideas

Phase 1로 이월:
- services.yaml schema (Zod) 정의 — KB-01
- services.yaml staleness detection — KB-03
- 로그 sanitization — KB-04
- 5개 tool wrapper의 나머지 4개 Zod schema

Phase 2로 이월:
- state/ git pre-commit hook (fast-forward only) — APPLY-07
- 2PC commit ordering 검증 — APPLY-04
- 5-key UI inline legend — APPLY-02

Future milestones (v2):
- NOTIFY-01 Discord webhook
- OSS-01 별도 repo 분리

---

*Generated by /gsd-discuss-phase 0 --auto*
*Auto-mode: Claude selected recommended option for each gray area without user prompts*
