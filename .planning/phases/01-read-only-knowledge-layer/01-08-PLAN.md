---
phase: 01-read-only-knowledge-layer
plan: 08
type: execute
wave: 4
depends_on:
  - 01-07
files_modified:
  - public/index.html
  - package.json
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
tags:
  - phase-1
  - frontend
  - htmx
  - sse
  - ui

must_haves:
  truths:
    - "public/index.html이 한 줄 input + 전송 버튼 + 6 SSE event handler를 가진다 (UI-01 + UI-02)"
    - "htmx 2.0.10 + htmx-ext-sse@2.2.4가 STACK.md 버전 lock으로 로드 (DESIGN.md correction #4)"
    - "tool-start / tool-result event가 #output에 .tool-event 카드로 시각화 (UI-03)"
    - "drift event가 #drift-banner 별도 div로 표시 (UI-04 + D-13.4)"
    - "error event가 #error-banner 별도 div + monospace fix 필드 verbatim (D-15.1 + D-15.2)"
    - "raw HTML 파싱 미사용 — textContent로 XSS 차단 (FRICTION #9 lesson)"
    - "다크 터미널 aesthetic — 01-UI-SPEC.md CSS custom properties 정확 반영"
  artifacts:
    - path: "public/index.html"
      provides: "단일 HTML — htmx form + 6 SSE handler + 다크 터미널 CSS tokens"
      contains: "sse-connect"
      min_lines: 150
    - path: "package.json"
      provides: "htmx.org 2.0.10 dependency 추가 (이미 htmx-ext-sse 존재 — htmx 자체 추가)"
      contains: "htmx.org"
  key_links:
    - from: "public/index.html"
      to: "src/server.ts /chat-stream"
      via: "htmx hx-post=/chat → response 안 sse-connect=/chat-stream"
      pattern: "sse-connect"
    - from: "public/index.html htmx:sseMessage"
      to: "agent/sse.ts 6 event types"
      via: "switch detail.type"
      pattern: "text-delta|tool-start|tool-result|final|error|drift"
---

<objective>
Phase 1 frontend — 단일 `public/index.html`로 한 줄 input + SSE 스트리밍 + 6 named event handling. 01-UI-SPEC.md 디자인 계약 그대로 구현 (다크 터미널 aesthetic, drift banner, error banner, tool event cards).

Purpose: UI-01 (한 줄 입력 + SSE) + UI-02 (5 named SSE event = drift 포함 6) + UI-03 (tool call 시각화) + UI-04 (drift warning header). D-15.1/D-15.2 (error envelope verbatim + 별도 banner). 운영자가 1번에 30초 내 결정할 수 있는 콘솔.
Output: public/index.html (단일 파일, ~150-200 lines), package.json에 htmx.org 추가.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-read-only-knowledge-layer/01-UI-SPEC.md
@.planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/00-bootstrap-spikes/FRICTION.md
@spikes/03-sse-roundtrip
@.planning/phases/01-read-only-knowledge-layer/01-07-SUMMARY.md

<interfaces>
<!-- Plan 06 agent/sse.ts SseEvent type names: -->
<!-- "text-delta" | "tool-start" | "tool-result" | "final" | "error" | "drift" -->

<!-- 01-UI-SPEC.md Element Inventory: -->
<!-- #drift-banner / #error-banner / #output / #status-bar / form#prompt + button -->

<!-- Plan 07 server.ts routes: -->
<!-- GET /                    → HTML -->
<!-- GET /static/:file         → htmx.min.js / sse.js -->
<!-- POST /chat                → response with sse-connect div (redirects htmx) -->
<!-- GET /chat-stream?prompt=  → SSE stream -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: package.json에 htmx.org dependency 추가 (DESIGN.md correction #4)</name>
  <files>package.json</files>
  <read_first>
    - package.json (현재)
    - .planning/research/STACK.md (htmx@2.0.10 lock)
  </read_first>
  <action>
    `package.json`의 `dependencies`에 `htmx.org` 추가:

    ```bash
    cd /home/gon/projects/gon/gons-works && bun add htmx.org@2.0.10
    ```

    (또는 manual edit) — package.json `dependencies`에 다음 라인:
    ```json
    "htmx.org": "2.0.10"
    ```

    검증: `node_modules/htmx.org/dist/htmx.min.js` 존재 확인 (server.ts /static/htmx.min.js가 이 경로 serve).

    PROHIBITED:
    - htmx 1.x 사용 (DESIGN.md correction #4 — 2.x lock)
    - CDN 직접 로드 (Phase 1은 로컬 정적 파일 — 오프라인 가능)
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && grep -c '"htmx.org": "2.0.10"' package.json && test -f node_modules/htmx.org/dist/htmx.min.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"htmx.org": "2.0.10"' package.json` >= 1
    - `grep -c '"htmx-ext-sse": "2.2.4"' package.json` >= 1 (이미 존재)
    - `test -f node_modules/htmx.org/dist/htmx.min.js` 성공 (exit 0)
    - `test -f node_modules/htmx-ext-sse/sse.js` 성공
  </acceptance_criteria>
  <done>htmx 2.0.10 + htmx-ext-sse 2.2.4 모두 node_modules에 설치되어 server.ts /static/이 serve 가능.</done>
</task>

<task type="auto">
  <name>Task 2: public/index.html — 다크 터미널 콘솔 + 6 SSE event handler + textContent XSS 방지 (UI-01..04)</name>
  <files>public/index.html</files>
  <read_first>
    - .planning/phases/01-read-only-knowledge-layer/01-UI-SPEC.md ENTIRE FILE (디자인 계약 — DOM 구조, CSS tokens, copywriting, interaction flow)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `public/index.html` 섹션 (line 838-924)
    - spikes/03-sse-roundtrip/index.html (htmx + sse.js 연결 패턴 — 2 event 베이스)
    - .planning/research/PITFALLS.md #6 (RAG injection — textContent 방어)
    - .planning/phases/00-bootstrap-spikes/FRICTION.md #9 (raw HTML 파싱 grep false positive — 코멘트 표기 주의)
  </read_first>
  <action>
    `public/index.html` — 01-UI-SPEC.md 디자인 계약 그대로 구현. PATTERNS.md 코드를 베이스로 6 event handling으로 확장:

    **CSS Custom Properties** (UI-SPEC.md "Color/Typography/Spacing" 정확 반영):
    - colors: `--color-bg #0f1117`, `--color-surface #1a1d27`, `--color-accent #4ade80`, `--color-error #ef4444`, `--color-text #e2e8f0`, `--color-muted #64748b`, `--color-mono #86efac`, `--color-border #2d3244`, `--color-warning #f59e0b` (drift banner border)
    - spacing: `--space-xs 4px`, `--space-sm 8px`, `--space-md 16px`, `--space-lg 24px`, `--space-xl 32px`
    - typography: `--font-size-label 13px`, `--font-size-body 15px`, `--font-size-mono 13px`, `--font-size-heading 17px`, `--font-weight-normal 400`, `--font-weight-semibold 600`, `--line-height-body 1.6`, `--line-height-label 1.4`, `--line-height-mono 1.5`, `--font-family-sans system-ui, -apple-system, BlinkMacSystemFont, sans-serif`, `--font-family-mono 'JetBrains Mono', 'Cascadia Code', Menlo, Consolas, monospace`

    **head: STACK.md lock 인용 + DESIGN.md correction #4 코멘트 + 2 script 로드** (htmx + sse extension은 별도 — DESIGN.md correction #4 lock):
    ```
    <script src="/static/htmx.min.js"></script>
    <script src="/static/sse.js"></script>
    ```

    **Body 구조 (UI-SPEC.md DOM 구조 그대로)** — `<main>` 안에 다음 순서:
    1. `<div id="drift-banner">` — 기본 display:none, sticky top:0 z-index:100, border 2px solid `--color-warning`, padding `--space-md`, margin-bottom `--space-lg`, font-size `--font-size-heading`, font-weight `--font-weight-semibold`
    2. `<div id="error-banner">` — 기본 display:none, background rgba(239,68,68,0.1), border 1px solid `--color-error`, .heading 클래스(font-weight semibold), .fix 클래스(font-family-mono, color #fca5a5, background rgba(0,0,0,0.3), white-space pre-wrap)
    3. `<div id="output">` — flex-grow 1, padding `--space-md`, border 1px solid `--color-border`, white-space pre-wrap, min-height 200px. 빈 상태(`:empty::before`) "운영자 질문을 입력하면 서비스 상태와 로그를 분석합니다." (italic, color `--color-muted`)
    4. `<div id="status-bar">` — color `--color-accent`, font-size `--font-size-label`, min-height 1.5em
    5. `<form hx-post="/chat" hx-target="#sse-mount" hx-swap="innerHTML">` — input + button
       - `<input id="prompt" name="prompt" type="text" autofocus autocomplete="off" placeholder="질문하세요. 예: ais-prod redis 어디 쓰여?">` (한국어 placeholder — UI-SPEC.md Copywriting Contract 그대로)
       - `<button type="submit">전송</button>` (UI-SPEC.md Primary CTA)
    6. `<div id="sse-mount" style="display:none">` — htmx가 POST /chat 응답을 swap → response 안에 `sse-connect` div가 들어감 → htmx-ext-sse가 process

    `.tool-event` 클래스: background `--color-surface`, border-left 3px solid `--color-accent`, padding `--space-sm var(--space-md)`, margin `--space-sm 0`, font-family-mono, font-size `--font-size-label`. `.tool-event.error`는 border-left-color `--color-error`.

    `input:focus`: border-color `--color-accent` + outline none.
    `button`: padding `--space-sm var(--space-lg)`, background transparent, border 1px solid `--color-accent`, color `--color-accent`, font-weight semibold, border-radius 4px.
    `button:hover`: background `--color-accent`, color `--color-bg`.

    **layout:**
    - `<main>`: max-width 900px, margin 0 auto, padding `--space-md var(--space-xl)`, min-height 100vh, display flex flex-direction column. (데스크탑 1440-1920px 기준 — UI-SPEC.md Out of Scope: 모바일 전용 UI)

    **JavaScript (vanilla — script tag 안)**:

    핵심 원칙: `textContent` + `createTextNode` + `createElement`만 사용. raw HTML 파싱 차단. (FRICTION #9 grep false positive 방지를 위해 식별자 패턴 사용 — UI-SPEC.md Anti-Template + 코드 보안 코멘트는 "raw HTML 파싱 비-사용"으로 표기.)

    1. **DOM refs:** outputEl, driftEl, errorEl, statusEl, formEl 변수 캐싱
    2. **htmx:beforeRequest 리스너 (form submit 직전):**
       - outputEl.replaceChildren() — 이전 출력 clear
       - errorEl.style.display = "none"; errorEl.replaceChildren()
       - statusEl.replaceChildren()
    3. **htmx:sseMessage 리스너 (document.body):**
       - JSON.parse(detail.data) → switch (parsed.type):
         - **text-delta:** outputEl.appendChild(document.createTextNode(String(parsed.delta || ""))) — 누적
         - **tool-start:** createElement("div").className = "tool-event"; element.dataset.toolName = parsed.name; element.textContent = "· " + parsed.name + " 호출 중..."; outputEl.appendChild
         - **tool-result:** outputEl.querySelectorAll('.tool-event[data-tool-name="...]')의 마지막 카드를 update — classList.toggle("error", !parsed.ok); textContent = (parsed.ok ? "✓ " : "✗ ") + parsed.name + " — " + (parsed.summary || "")
         - **final:** statusEl.textContent = "✓ 완료"; setTimeout(() => statusEl.textContent = "", 1500) — 1.5초 후 fade
         - **error:** errorEl.style.display = "block"; replaceChildren(); 4 child element 추가:
           - .heading div: textContent = parsed.problem
           - cause div: textContent = "원인: " + parsed.cause
           - fixLabel div: textContent = "해결 방법:"
           - .fix div: textContent = String(parsed.fix || "") — **D-15.1: verbatim, textContent로만 (영어 셸 명령 그대로)**
         - **drift:** driftEl.style.display = "block"; replaceChildren(); 2 child:
           - heading div: textContent = "⚠ services.yaml 불일치"
           - body div: textContent = "docker-only " + (parsed.unknown?.length || 0) + "개, yaml-only " + (parsed.stale?.length || 0) + "개 발견 — services.yaml 갱신이 필요합니다."

    **Input cleansing:** 모든 외부 데이터(text-delta delta, tool args, error fix, drift unknown/stale)는 textContent로만 DOM 추가. raw HTML 문자열을 DOM에 삽입하는 패턴 미사용 (XSS 차단 — PITFALL #6 RAG injection도 frontend 단에서 차단).

    **PROHIBITED (FRICTION #9 — grep false positive 회피):**
    - HTML-as-string DOM 삽입 패턴 (raw HTML parsing) — XSS 위험. PATTERNS.md analog의 `replaceChildren` + `createTextNode` 패턴만.
    - htmx 1.x 또는 SSE 내장 시도 (DESIGN.md correction #4 위반).
    - 보안 코멘트에서 위험 식별자를 substring으로 적지 마라 — `grep -v '^//' public/index.html | grep -ciE '\.inner[hH]tml\s*[=(]'`이 0이어야 acceptance 통과 (FRICTION #9 lesson). 코멘트가 필요하면 "raw HTML 파싱 비-사용" 표현 사용.
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && grep -c 'sse-connect' public/index.html && grep -v '^[[:space:]]*//' public/index.html | grep -v '^[[:space:]]*\*' | grep -v '^[[:space:]]*<!--' | grep -ciE '\.inner[hH]tml\s*[=(]' || echo zero</automated>
  </verify>
  <acceptance_criteria>
    - File `public/index.html` exists and is >= 150 lines
    - `grep -c 'sse-connect\|sse-swap' public/index.html` >= 1 (htmx-ext-sse 활용)
    - `grep -c '/static/htmx.min.js' public/index.html` >= 1
    - `grep -c '/static/sse.js' public/index.html` >= 1 (DESIGN.md correction #4 — 별도 로드)
    - `grep -c 'text-delta' public/index.html` >= 1 (UI-02 6 event)
    - `grep -c 'tool-start' public/index.html` >= 1
    - `grep -c 'tool-result' public/index.html` >= 1
    - `grep -c '"final"' public/index.html` >= 1 (case label)
    - `grep -c '"error"' public/index.html` >= 1
    - `grep -c '"drift"' public/index.html` >= 1 (UI-04)
    - `grep -c '#drift-banner\|drift-banner' public/index.html` >= 2 (CSS + DOM)
    - `grep -c '#error-banner\|error-banner' public/index.html` >= 2
    - **XSS check (FRICTION #9 식별자 패턴):** `grep -v '^[[:space:]]*//' public/index.html | grep -v '^[[:space:]]*\*' | grep -v '^[[:space:]]*<!--' | grep -ciE '\.inner[hH]tml\s*[=(]' == 0` (코드 위치에서 위험 식별자 사용 0건)
    - `grep -c 'createTextNode\|createElement' public/index.html` >= 4 (textContent 패턴 사용)
    - `grep -c '전송\|질문하세요' public/index.html` >= 2 (한국어 copywriting)
    - `grep -c '\-\-color-bg\|\-\-color-accent' public/index.html` >= 2 (CSS custom properties)
    - `grep -c '\-\-font-family-mono' public/index.html` >= 1 (UI-SPEC typography)
  </acceptance_criteria>
  <done>public/index.html 단일 파일에 6 SSE event handler + 다크 터미널 CSS + UI-04 drift banner + D-15.2 error banner + textContent XSS 방어. UI-SPEC.md 디자인 계약 정확 반영.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE event data → DOM | LLM/server가 보낸 text-delta, tool args, error fix는 untrusted (LLM hallucination 또는 PITFALL #6 log injection). textContent 외 사용 금지. |
| readLogs 출력 → text-delta 스트림 | sanitizeLogs() (Plan 04)가 server-side에서 SECRET/INJECTION 차단. frontend는 추가 파싱 없음. |
| browser ↔ server (127.0.0.1) | 같은 머신 — Plan 07의 hostname lock에 의존. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Tampering (XSS via SSE delta) | LLM 응답에 script 태그 raw HTML 포함 → HTML-as-string DOM 삽입 시 실행 | mitigate | textContent + createTextNode + createElement만 사용. acceptance grep으로 위험 식별자 부재 강제 (FRICTION #9 식별자 패턴). |
| T-01-08-02 | Tampering (XSS via error fix) | error envelope의 fix 필드에 LLM이 HTML 작성 | mitigate | error 핸들러가 textContent로만 fix 표시 (D-15.1 — verbatim 셸 명령은 텍스트로만 의미). |
| T-01-08-03 | Tampering (XSS via tool name) | tool name 자체가 untrusted (그러나 우리 코드 내부 이름) | accept | TOOL_SCHEMAS는 server-side 정의 — LLM은 임의 tool name 부여 불가. |
| T-01-08-04 | DoS (UI freeze) | text-delta 폭발(수천 chunk) → DOM 거대화 | accept (Phase 1) | 운영자 1명 + 1-shot UX. 새 prompt 시 outputEl.replaceChildren()으로 reset. Phase 2 considers. |
| T-01-08-05 | Information Disclosure (history clear) | 이전 prompt의 응답이 새 prompt 시 사라짐 — 사용자가 의도하지 않은 리셋 | accept | UI-SPEC.md Interaction Contract — "재연결 시 #output 내용은 초기화하지 않음 (누적 유지)"는 SSE 재연결 케이스. 새 prompt submit은 의도적 reset. |
| T-01-08-06 | Tampering (htmx-ext-sse 자동 재연결 dup) | 재연결 시 server가 같은 prompt를 두 번 처리 (PITFALL #4) | mitigate | Plan 06 inFlight Map dedup. frontend는 재연결 시 단순 SSE 수신 — no retry orchestration. |
</threat_model>

<verification>
- (Live smoke — Plan 09에서) `bun run dev` 후 브라우저로 http://127.0.0.1:3000 → form 표시 + 다크 터미널 aesthetic 확인
- prompt "ais-prod redis 어디 쓰여?" 입력 → SSE 스트림 시작:
  - tool-start 카드 표시 ("· listContainers 호출 중...")
  - tool-result 카드 업데이트 (✓ / ✗)
  - text-delta 누적
  - final 표시 ("✓ 완료" 1.5초 후 fade)
- (drift 인위적 시연 — Plan 09) services.yaml에서 한 stack 삭제 후 query → drift banner 표시 (⚠ + N개 stale)
- (error 시연 — Plan 09) `unset DOCKER_CONTEXT && bun run dev`로 시작 → boot 단계에서 error envelope 출력
- 브라우저 DevTools Console: error 없음 (htmx + sse.js 정상 로드)
- DevTools Network: GET /chat-stream Content-Type: text/event-stream
</verification>

<success_criteria>
- UI-01 충족: 한 줄 input form + SSE 스트리밍 응답
- UI-02 충족: 6 named SSE event(text-delta/tool-start/tool-result/final/error/drift) 모두 핸들링
- UI-03 충족: tool call 시각화 (.tool-event 카드 — 진행 점 + 결과 표시)
- UI-04 충족: drift warning이 #drift-banner에 표시 (sticky top header)
- D-15.1 충족: error envelope의 fix 필드 verbatim (textContent로 monospace 표시)
- D-15.2 충족: error는 #error-banner 별도 div, 빨간 배경
- DESIGN.md correction #4 준수: htmx 2.x + htmx-ext-sse 별도 로드
- FRICTION #9 lesson: raw HTML 파싱 패턴 부재 (위험 식별자 grep 0건)
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-only-knowledge-layer/01-08-SUMMARY.md` with:
- public/index.html DOM 구조 + 6 event handler 매핑 표
- UI-SPEC.md CSS custom properties 반영 검증 (color/spacing/typography 토큰 list)
- 라이브 라우팅 검증 (POST /chat → /chat-stream sse-connect 시퀀스)
- 마찰 (FRICTION carry-over)
</output>
</content>
</invoke>