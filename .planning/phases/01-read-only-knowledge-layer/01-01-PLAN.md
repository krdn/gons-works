---
phase: 01-read-only-knowledge-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kb/schema.ts
  - scripts/draft-services-yaml.ts
  - state/services.yaml
  - state/services.yaml.draft
  - package.json
autonomous: false
requirements:
  - KB-01
tags:
  - phase-1
  - knowledge-layer
  - rag
  - services-yaml
  - augmentation

must_haves:
  truths:
    - "state/services.yaml의 5 stack 모두 4 슬롯(depends_on/volumes/normal_log_pattern/key_log_locations)이 비-empty 값으로 채워져 있다."
    - "Zod schema(kb/schema.ts)가 보강된 services.yaml을 strict 모드로 parse 성공한다."
    - "운영자가 draft → live diff를 검토 후 mv + git commit으로 명시적 승인했다."
  artifacts:
    - path: "kb/schema.ts"
      provides: "ServicesYamlSchema + StackSchema (Zod v4) — RAG indexing의 input 타입"
      exports:
        - ServicesYamlSchema
        - StackSchema
        - ContainerRefSchema
      contains: "z.object"
      min_lines: 25
    - path: "scripts/draft-services-yaml.ts"
      provides: "AI 초안 생성 + Zod 검증 + jsdiff createPatch 출력"
      contains: "createPatch"
    - path: "state/services.yaml"
      provides: "5 stack × 4 슬롯이 보강된 도메인 지식 (Phase 1 RAG의 ground truth)"
      contains: "key_log_locations:"
  key_links:
    - from: "scripts/draft-services-yaml.ts"
      to: "kb/schema.ts"
      via: "import { ServicesYamlSchema } from '../kb/schema'"
      pattern: "ServicesYamlSchema"
    - from: "scripts/draft-services-yaml.ts"
      to: "diff package createPatch"
      via: "import { createPatch } from 'diff'"
      pattern: "createPatch\\(.*services\\.yaml"
---

<objective>
Phase 1 RAG indexing의 prerequisite — `state/services.yaml`의 5 stack × 4 TODO 슬롯을 AI 초안으로 채우고 사용자 review 후 commit. 동시에 RAG/staleness 처리 코드의 입력 타입(`kb/schema.ts`)을 정의한다.

Purpose: services.yaml 보강을 미루고 빈 슬롯으로 RAG 인덱싱하면 Spike 2 baseline(top-1 sim=0.6308)에 미달한다. 이 plan이 Wave 2의 KB indexing(`01-05`)의 데이터 품질 baseline을 잡는다. (D-12 lock)
Output: kb/schema.ts (Zod), scripts/draft-services-yaml.ts에 schema 검증 + diff 출력 추가, state/services.yaml에 4 슬롯 채워진 commit, package.json에 `draft-yaml` 스크립트 alias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md
@.planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@scripts/draft-services-yaml.ts
@state/services.yaml
@state/services.yaml.review-checklist.md
@src/env.ts

<interfaces>
<!-- src/env.ts (PATTERNS.md re-use): -->
```typescript
export interface EnvProblem { problem: string; cause: string; fix: string }
export function loadEnv(): Env  // BOOT-04/05 startup guard
```

<!-- 기존 scripts/draft-services-yaml.ts 첫 줄: -->
```typescript
import { loadEnv } from "../src/env"
const env = loadEnv()
const ctx = env.DOCKER_CONTEXT  // home-server (FRICTION #10)
```

<!-- 기존 services.yaml 구조 (5 stacks: news/ais/n8n/open-webui/krdn-fx) — 모두 4 TODO 슬롯 비어있음 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: kb/schema.ts — Zod v4 ServicesYamlSchema 정의 (D-12.4 strict)</name>
  <files>kb/schema.ts, kb/schema.test.ts</files>
  <read_first>
    - src/env.ts (Zod v4 패턴: z.object + .default() + safeParse + z.infer)
    - state/services.yaml (현재 구조 + 5 stack + unmatched_containers)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `kb/schema.ts` 섹션
    - .planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md D-12.4
    - .planning/research/STACK.md (Zod v4 + z.toJSONSchema 호환성 — DESIGN.md correction #3)
  </read_first>
  <behavior>
    - parse 성공: 4 슬롯 채워진 valid yaml → ServicesYamlSchema.parse(obj) returns typed object
    - parse 실패: depends_on가 string 아닌 number → ZodError throw
    - default 적용: 4 슬롯 빈 yaml → strict 모드는 .default([])/.default("") 의존
    - stacks key 자유: Record<string, StackSchema> 사용 (키 강제 안 함)
    - unmatched_containers는 optional (D-12.4: RAG indexing은 stacks만)
  </behavior>
  <action>
    `kb/schema.ts` 생성 — Zod v4 schema (D-12.4 strict). PATTERNS.md `kb/schema.ts` 섹션의 코드를 그대로 채택:

    ```typescript
    import { z } from "zod"

    export const ContainerRefSchema = z.object({
      name: z.string(),
      image: z.string(),
      state: z.string(),
      status: z.string().optional(),
    })

    export const StackSchema = z.object({
      purpose: z.string().min(1),
      depends_on: z.array(z.string()).default([]),
      volumes: z.array(z.string()).default([]),
      normal_log_pattern: z.string().default(""),
      key_log_locations: z.array(z.string()).default([]),
      containers: z.array(ContainerRefSchema).default([]),
    })

    export const ServicesYamlSchema = z.object({
      generated_at: z.string(),
      generated_from: z.string(),
      stacks: z.record(z.string(), StackSchema),
      unmatched_containers: z.array(ContainerRefSchema).optional(),
    })

    export type ServicesYaml = z.infer<typeof ServicesYamlSchema>
    export type Stack = z.infer<typeof StackSchema>
    export type ContainerRef = z.infer<typeof ContainerRefSchema>
    ```

    `kb/schema.test.ts` 생성 — RED→GREEN:
    1. parse 성공 테스트: 모든 슬롯 채워진 valid yaml object → expect.toBeDefined()
    2. parse 실패 테스트: depends_on에 number 포함 → expect(...).toThrow()
    3. default 적용: 빈 stacks entry → parse 후 depends_on=[], normal_log_pattern=""

    PATTERNS.md 인용 위반 금지: `z.toJSONSchema`는 이 plan에서 사용하지 않음 (Plan 02 tools/_index.ts에서 사용).
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && bun test kb/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `kb/schema.ts` exists with `grep -c 'export const ServicesYamlSchema' kb/schema.ts` >= 1
    - `grep -c 'export const StackSchema' kb/schema.ts` >= 1
    - `grep -c 'export type ServicesYaml' kb/schema.ts` >= 1
    - `grep -c 'z.record' kb/schema.ts` >= 1 (D-12.4 stacks Record)
    - `grep -c 'unmatched_containers' kb/schema.ts` >= 1 with `.optional()` (D-12.4)
    - `bun test kb/schema.test.ts` exits 0 with at least 3 tests pass
  </acceptance_criteria>
  <done>kb/schema.ts exports compile and Zod schema parses both populated and TODO-empty services.yaml shapes; tests RED → GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/draft-services-yaml.ts에 Zod strict 검증 + jsdiff createPatch 추가 (D-12.3, D-12.4)</name>
  <files>scripts/draft-services-yaml.ts, package.json</files>
  <read_first>
    - scripts/draft-services-yaml.ts (현재 구현 — 보존, 수정 최소화)
    - kb/schema.ts (Task 1 결과물)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `scripts/draft-services-yaml.ts` 섹션 (line 927-958)
    - .planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md D-12.2 + D-12.3
    - state/services.yaml.review-checklist.md (review 가이드 참조)
    - .planning/research/STACK.md (`diff@9.0.0` createPatch import 패턴)
  </read_first>
  <action>
    기존 `scripts/draft-services-yaml.ts`는 **rewrite 금지** (D-12.2 — 기존 보존). 마지막 단계에만 추가:

    1. **Zod strict 검증** (D-12.4):
       - import: `import { ServicesYamlSchema } from "../kb/schema"`
       - import: `import { load } from "js-yaml"` *(미설치 시 `bun add js-yaml @types/js-yaml`)*
       - draft 출력 직전: `const parsed = ServicesYamlSchema.safeParse(load(output))` → 실패 시 `console.warn("[draft-services] ⚠ 스키마 검증 경고:", parsed.error.issues)` (탈락 아닌 경고. AI 초안이 4 슬롯을 비어있게 두면 default가 적용되어 parse 통과).

    2. **Diff 출력** (D-12.3):
       - import: `import { createPatch } from "diff"`
       - draft string 생성 후:
         ```typescript
         try {
           const current = await Bun.file("state/services.yaml").text()
           const patch = createPatch("services.yaml", current, output, "current", "draft")
           console.log("\n=== Unified Diff (current → draft) ===")
           console.log(patch)
         } catch {
           console.log("[draft-services] 현재 services.yaml 없음 — diff 생략")
         }
         console.log("Review then: mv state/services.yaml.draft state/services.yaml")
         ```

    3. **출력 파일명**: 기존이 `state/services.yaml`을 직접 덮어쓴다면 **반드시 `state/services.yaml.draft`로 변경** (D-12.3 — 운영자가 명시적으로 mv).

    4. **package.json scripts 추가** (D-12.2 — 별칭):
       ```json
       "scripts": {
         ...
         "draft-yaml": "bun run scripts/draft-services-yaml.ts"
       }
       ```

    PROHIBITED: 기존 LLM 호출 로직을 다시 작성하지 마라. 마지막 emit 단계에 4-5줄 추가만.

    **5 stack × 4 슬롯 LLM prompt** (existing 또는 보강 — 기존 prompt가 이미 4 슬롯을 채우라는 지시인지 확인):
    - depends_on (e.g., news → news-postgres(5433), news-prod-redis(6380))
    - volumes (named volume / bind mount 경로)
    - normal_log_pattern (정상 동작 시 자주 보이는 로그 한 줄 예시)
    - key_log_locations (디버깅 시 봐야 하는 컨테이너 이름들)
    이미 prompt에 명시되어 있으면 변경 없이 유지.
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works &amp;&amp; bun run scripts/draft-services-yaml.ts 2&gt;&amp;1 | grep -E "(Unified Diff|services.yaml.draft|Review then)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'createPatch' scripts/draft-services-yaml.ts` >= 1
    - `grep -c 'ServicesYamlSchema' scripts/draft-services-yaml.ts` >= 1
    - `grep -c 'services.yaml.draft' scripts/draft-services-yaml.ts` >= 1
    - `grep -c 'Review then' scripts/draft-services-yaml.ts` >= 1
    - `grep -c 'draft-yaml' package.json` >= 1 (script alias)
    - 실행 시 stdout에 "=== Unified Diff" 라인 출력 (state/services.yaml 존재 시)
    - 실행 시 `state/services.yaml.draft` 파일이 생성됨 (직접 덮어쓰지 않음)
  </acceptance_criteria>
  <done>스크립트가 LLM 호출 결과를 services.yaml.draft로 emit하고 unified diff를 stdout에 출력. ServicesYamlSchema가 strict 모드로 검증.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 운영자 review + mv + git commit (D-12.1 + D-12.3)</name>
  <files>state/services.yaml</files>
  <what-built>
    Claude가 `bun run draft-yaml`을 실행하여 `state/services.yaml.draft`를 생성하고 unified diff를 stdout에 출력했다. 운영자가 5 stack × 4 슬롯의 AI 보강 내용을 review하고 직접 mv + git commit으로 lock한다 (D-12.1: AI 초안 + 사용자 review 패턴).
  </what-built>
  <how-to-verify>
    Claude 측에서 미리 자동화 (이 checkpoint 진입 전):
    1. `unset ANTHROPIC_API_KEY` (FRICTION #8 — 셸 빈 값이 .env를 덮어쓰지 않도록)
    2. `bun run draft-yaml` 실행, stdout 확인
    3. `state/services.yaml.draft` 파일 존재 확인
    4. unified diff stdout 출력을 사용자에게 그대로 표시

    운영자 검증 단계:
    1. stdout의 unified diff를 review — 5 stack 모두 다음 4 슬롯이 비-empty인지:
       - depends_on: 명확한 컨테이너/포트 의존성
       - volumes: 실제 명시된 named volume / bind mount 경로
       - normal_log_pattern: 정상 동작 시의 한 줄 예시
       - key_log_locations: 디버깅 시 봐야 하는 컨테이너 이름들
    2. 운영자 본인이 알고 있는 사실과 모순 없는지 확인 (D-12 review-checklist.md 참조)
    3. 잘못된 항목은 운영자가 직접 `state/services.yaml.draft` 편집
    4. 승인 시 다음 명령으로 lock:
       ```
       mv state/services.yaml.draft state/services.yaml
       git -C . add state/services.yaml
       git -C . commit -m "docs(01): services.yaml 5 stack 4 슬롯 보강 — KB-01 prerequisite"
       ```
    5. 거부 시 `state/services.yaml.draft`를 손으로 편집 후 재시도, 또는 보강 내용을 다시 작성하라고 Claude에 지시
  </how-to-verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s state/services.yaml | grep -c "보강"` >= 1 OR commit message가 KB-01 / 4 슬롯 / 보강 중 하나 키워드 포함
    - `grep -c 'TODO' state/services.yaml` == 0 OR `grep -v '^#' state/services.yaml | grep -c 'TODO'` == 0 (4 슬롯 TODO 마커 모두 제거됨)
    - `grep -A2 'depends_on:' state/services.yaml | grep -v '^--' | grep -v '^[[:space:]]*$' | grep -v '^[[:space:]]*depends_on: \[\]' | wc -l` > 0 (최소 1개 stack의 depends_on이 채워짐 — full check은 verify 단계에서)
    - state/services.yaml.draft 파일은 **삭제됨** (mv로 대체됨)
    - `bun -e "import {ServicesYamlSchema} from './kb/schema'; import {load} from 'js-yaml'; ServicesYamlSchema.parse(load(await Bun.file('state/services.yaml').text())); console.log('OK')"` 출력 "OK"
  </acceptance_criteria>
  <resume-signal>
    "approved" 입력 → 다음 wave 진행 / 또는 issue 기재 후 Task 2 재실행 요청
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude(LLM) → AI 초안 | LLM이 docker ps + readLogs 샘플 + readCompose 샘플을 받아 services.yaml 슬롯 채움. 출력은 검증 전 untrusted. |
| stdout → 운영자 review | unified diff를 운영자가 읽고 mv 결정. semantic 검증의 마지막 게이트. |
| state/services.yaml → KB-02 RAG indexing (Plan 05) | 보강된 yaml은 cosine similarity baseline을 정의. 빈 슬롯은 RAG quality gap 0.32 미달. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | LLM 초안에 secret(API_KEY/PASSWORD) hallucinate 가능 | mitigate | 운영자 review (Task 3 checkpoint)가 명시 게이트. PITFALL #6 sanitization은 Plan 04 readLogs.ts에서 input 단에 처리. |
| T-01-01-02 | Information Disclosure | scripts/draft-services-yaml.ts가 LLM에 readLogs 샘플 전달 시 secret 노출 가능 | mitigate | 스크립트가 LLM에 전달하는 readLogs 샘플은 sanitizeLogs() (Plan 04 — 이미 존재) 거친 후 전달. **draft-services-yaml.ts script 수정 시 이 sanitization 단계가 이미 호출되는지 확인.** 미존재 시 Task 2에서 inline regex 적용. |
| T-01-01-03 | Spoofing | git commit author/co-author 위조 (PITFALL #9) | accept | Phase 1은 사용자 직접 git commit (Claude는 명령 안내만). Phase 2에서 sanitizeCommitMsg 도입. |
| T-01-01-04 | Tampering | services.yaml.draft를 다른 프로세스가 mv 시점에 변경 | accept | 단일 사용자 단일 머신, race window 매우 작음. mv는 atomic. |
| T-01-01-05 | DoS | 빈 환경 변수가 .env를 덮어씀 (FRICTION #8) | mitigate | 워크어라운드: 운영자가 `unset ANTHROPIC_API_KEY` 실행 (Task 3 how-to-verify에 명시). startup guard는 Plan 07 server.ts에서 친절한 에러 메시지 추가. |
</threat_model>

<verification>
- `bun test kb/schema.test.ts` 통과
- `bun run draft-yaml` 실행 시 stdout에 unified diff 출력 + `state/services.yaml.draft` 생성
- 운영자 mv + commit 후 `git log -1 --pretty=%H state/services.yaml`이 새 SHA 출력
- `bun -e "import {ServicesYamlSchema} from './kb/schema'; import {load} from 'js-yaml'; ServicesYamlSchema.parse(load(await Bun.file('state/services.yaml').text())); console.log('OK')"`가 "OK" 출력
- 5 stack 모두 4 슬롯 비-empty (수동 검증 + 자동 grep)
</verification>

<success_criteria>
- KB-01 prerequisite 충족: services.yaml 4 슬롯 보강 + git commit 기록
- kb/schema.ts strict Zod schema 정의 — Plan 05 KB-02/KB-03이 import 가능
- D-12.1/D-12.2/D-12.3/D-12.4 모두 충족 (AI draft + script 분리 + diff + Zod strict)
- Plan 05 RAG indexing이 baseline sim=0.6308 이상 retrieval 품질 달성 가능 (downstream)
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-only-knowledge-layer/01-01-SUMMARY.md` with:
- 보강된 services.yaml 5 stack × 4 슬롯 요약 (테이블)
- AI 초안 vs 운영자 수정 사항 (있다면)
- kb/schema.ts export 목록
- 마찰 (FRICTION carry-over) — 있으면 .planning/phases/01-*/FRICTION.md 신설
</output>
</content>
</invoke>