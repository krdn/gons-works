---
phase: 01-read-only-knowledge-layer
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - audit/schema.sql
  - audit/log.ts
  - audit/log.test.ts
  - .gitignore
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-03
tags:
  - phase-1
  - audit
  - sqlite
  - cost-monitoring

must_haves:
  truths:
    - "data/copilot.db 가 bun:sqlite로 strict + safeIntegers 모드로 열린다 (PITFALL #10)"
    - "events 테이블이 turn row + child tool row hybrid 구조 (D-14.1) — parent_id 자기참조"
    - "audit/log.ts beginTurn / logTool / endTurn 3 함수가 commit하면 SQLite events 테이블에 row가 INSERT된다"
    - "/data/는 .gitignore에 등재되어 binary DB가 git에 들어가지 않는다 (D-14.2)"
  artifacts:
    - path: "audit/schema.sql"
      provides: "events 테이블 hybrid schema (D-14.1) + 2 인덱스 (parent_id, ts) + WAL/synchronous pragma 안내 주석"
      contains: "parent_id INTEGER REFERENCES events(id)"
      min_lines: 20
    - path: "audit/log.ts"
      provides: "beginTurn / logTool / endTurn API + Database singleton + WAL/strict open"
      exports:
        - beginTurn
        - logTool
        - endTurn
        - getDb
      contains: "strict: true"
    - path: ".gitignore"
      provides: "data/ 디렉토리 ignore 등재 (D-14.2)"
      contains: "/data/"
  key_links:
    - from: "audit/log.ts"
      to: "audit/schema.sql"
      via: "Bun.file('audit/schema.sql').text() → db.run(sql)"
      pattern: "audit/schema\\.sql"
    - from: "audit/log.ts Database constructor"
      to: "PITFALL #10 strict mode"
      via: "{ strict: true, safeIntegers: true, create: true }"
      pattern: "strict: true"
---

<objective>
SQLite event log 인프라 구축 — D-14 lock 5 결정 모두 구현. cost monitoring (token usage) + 디버깅 (tool 호출 이력) + Phase 1 audit trail의 SSOT가 된다 (state/는 Phase 2에서 git audit으로 활용).

Purpose: AUDIT-01 ("모든 read 도구 호출 기록") + AUDIT-03 ("timestamp/model/token usage/tool call 횟수 저장") 모두 cover. PITFALL #10 (bun:sqlite 묵음 NULL binding)을 architectural level에서 차단.
Output: audit/schema.sql (CREATE TABLE), audit/log.ts (3 함수 + db singleton), .gitignore에 /data/ 등재.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md
@.planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- D-14.3 row 필드 (CONTEXT.md): -->
<!-- turn row (parent_id NULL): id, ts, model, prompt(<=2000자 trim), input_tokens, output_tokens, cache_read_tokens, total_tool_calls, duration_ms, error_envelope (JSON or NULL) -->
<!-- tool row (parent_id NOT NULL): id, parent_id, ts, tool_name, input (JSON), result_summary(<=500자 trim), ok (BOOL), duration_ms -->

<!-- bun:sqlite open 패턴 (PITFALLS.md #10 + STACK.md): -->
<!-- new Database('./data/copilot.db', { strict: true, safeIntegers: true, create: true }) -->
<!-- db.run("PRAGMA journal_mode = WAL") -->
<!-- db.run("PRAGMA synchronous = NORMAL") -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: audit/schema.sql — events 테이블 hybrid schema (D-14.1) + .gitignore 갱신 (D-14.2)</name>
  <files>audit/schema.sql, .gitignore</files>
  <read_first>
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `audit/schema.sql` 섹션 (line 745-779)
    - .planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md D-14.1 + D-14.2 + D-14.3
    - .planning/research/PITFALLS.md #10 (bun:sqlite strict mode + WAL)
    - .planning/research/STACK.md (bun:sqlite line 165-178)
  </read_first>
  <action>
    `audit/schema.sql` 생성 — D-14.1 hybrid + D-14.3 minimum field set:

    ```sql
    -- audit/schema.sql (D-14.1 hybrid: turn row + child tool row)
    -- Open with bun:sqlite { strict: true, safeIntegers: true, create: true } (PITFALL #10)
    -- After CREATE TABLE: db.run("PRAGMA journal_mode = WAL"); db.run("PRAGMA synchronous = NORMAL")

    CREATE TABLE IF NOT EXISTS events (
      id                 INTEGER PRIMARY KEY AUTOINCREMENT,
      ts                 TEXT    NOT NULL,
      -- Turn row (parent_id NULL) — D-14.3 minimum
      model              TEXT,
      prompt             TEXT,
      input_tokens       INTEGER,
      output_tokens      INTEGER,
      cache_read_tokens  INTEGER,
      total_tool_calls   INTEGER,
      duration_ms        INTEGER,
      error_envelope     TEXT,
      -- Tool row (parent_id NOT NULL) — D-14.3 minimum
      parent_id          INTEGER REFERENCES events(id),
      tool_name          TEXT,
      input              TEXT,
      result_summary     TEXT,
      ok                 INTEGER
    );

    CREATE INDEX IF NOT EXISTS idx_events_parent_id ON events(parent_id);
    CREATE INDEX IF NOT EXISTS idx_events_ts ON events(ts);
    ```

    `.gitignore`에 다음 라인 추가 (D-14.2 — 이미 존재 시 skip):
    ```
    # data/ (D-14.2): bun:sqlite copilot.db — binary, not git-tracked
    /data/
    ```

    PROHIBITED:
    - `prompt VARCHAR(2000)` 또는 `result_summary VARCHAR(500)` — SQLite는 컬럼 길이 강제 안 함. trim은 audit/log.ts의 application layer에서 (D-14.3).
    - LOOP-07 idempotency_key 컬럼 추가 — D-14.3 minimum + Claude's Discretion 결정 보류 (Plan 06 agent/loop.ts에서 in-memory Map으로 처리).
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && grep -c 'parent_id INTEGER REFERENCES events(id)' audit/schema.sql && grep -c '^/data/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/schema.sql` exists; `grep -c 'CREATE TABLE IF NOT EXISTS events' audit/schema.sql` >= 1
    - `grep -c 'parent_id INTEGER REFERENCES events(id)' audit/schema.sql` >= 1 (D-14.1 hybrid 자기참조)
    - `grep -c 'idx_events_parent_id' audit/schema.sql` >= 1 (인덱스)
    - `grep -c 'idx_events_ts' audit/schema.sql` >= 1 (인덱스)
    - `grep -c 'error_envelope' audit/schema.sql` >= 1 (D-14.3 turn row)
    - `grep -c 'tool_name' audit/schema.sql` >= 1 (D-14.3 tool row)
    - `grep -c 'idempotency_key' audit/schema.sql` == 0 (D-14.3 minimum + LOOP-07 plan 06으로 deferred)
    - `.gitignore`에 `/data/` 라인 존재: `grep -c '^/data/' .gitignore` >= 1
  </acceptance_criteria>
  <done>events 테이블 schema가 D-14.1 hybrid + D-14.3 minimum field set으로 정의됨. .gitignore가 /data/ ignore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: audit/log.ts — beginTurn / logTool / endTurn API + Database singleton (PITFALL #10 + D-14.3)</name>
  <files>audit/log.ts, audit/log.test.ts</files>
  <read_first>
    - audit/schema.sql (Task 1 산출물)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `audit/log.ts` 섹션 (line 783-830)
    - .planning/research/PITFALLS.md #10 prevention block (line 462-481)
    - .planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md D-14.3
  </read_first>
  <behavior>
    - getDb(): 첫 호출 시 ./data/copilot.db 디렉토리 mkdir + Database open (strict + safeIntegers + create) + audit/schema.sql 실행 + WAL pragma. 이후 호출은 동일 instance 반환 (singleton).
    - beginTurn("사용자 질문", "claude-sonnet-4-6") → bigint id 반환. events 테이블에 parent_id NULL row INSERT.
    - beginTurn: prompt 길이 2000자 초과 시 2000자로 trim (D-14.3)
    - logTool(parentId, "listContainers", { limit: 5 }, "12 컨테이너", true, 250) → events 테이블에 parent_id NOT NULL row INSERT.
    - logTool: result_summary 500자 초과 시 500자 trim. input은 JSON.stringify (PITFALL #10 — strict 모드에서 binding key 오타 throw).
    - endTurn(turnId, output_tokens, cache_read_tokens, total_tool_calls, duration_ms, errorEnvelope?) → UPDATE turn row.
    - PITFALL #10 verify: `db.prepare("SELECT * FROM events WHERE id = $missing")` 호출 시 binding 누락 → throw (strict 모드 활성).
  </behavior>
  <action>
    `audit/log.ts` 생성 — PATTERNS.md `audit/log.ts` 섹션 + PITFALL #10 prevention block 채택:

    ```typescript
    import { Database } from "bun:sqlite"
    import { mkdirSync, existsSync } from "node:fs"
    import { dirname } from "node:path"

    const DB_PATH = "./data/copilot.db"
    let _db: Database | null = null

    export function getDb(): Database {
      if (_db) return _db
      const dir = dirname(DB_PATH)
      if (!existsSync(dir)) mkdirSync(dir, { recursive: true })
      // PITFALL #10: strict + safeIntegers 필수
      _db = new Database(DB_PATH, { strict: true, safeIntegers: true, create: true })
      _db.run("PRAGMA journal_mode = WAL")
      _db.run("PRAGMA synchronous = NORMAL")
      // schema.sql 실행 (idempotent — IF NOT EXISTS)
      const sql = require("fs").readFileSync("audit/schema.sql", "utf-8") as string
      _db.run(sql)
      return _db
    }

    export function beginTurn(prompt: string, model: string): bigint {
      const db = getDb()
      const trimmed = prompt.slice(0, 2000)  // D-14.3
      const stmt = db.prepare(
        "INSERT INTO events (ts, model, prompt) VALUES ($ts, $model, $prompt)"
      )
      stmt.run({ $ts: new Date().toISOString(), $model: model, $prompt: trimmed })
      const row = db.query("SELECT last_insert_rowid() AS id").get() as { id: bigint }
      return row.id
    }

    export function logTool(
      parentId: bigint,
      name: string,
      input: unknown,
      resultSummary: string,
      ok: boolean,
      durationMs: number
    ): void {
      const db = getDb()
      const stmt = db.prepare(
        "INSERT INTO events (ts, parent_id, tool_name, input, result_summary, ok, duration_ms) " +
        "VALUES ($ts, $pid, $name, $input, $summary, $ok, $dur)"
      )
      stmt.run({
        $ts: new Date().toISOString(),
        $pid: parentId,
        $name: name,
        $input: JSON.stringify(input).slice(0, 2000),
        $summary: resultSummary.slice(0, 500),  // D-14.3
        $ok: ok ? 1 : 0,
        $dur: durationMs,
      })
    }

    export function endTurn(
      id: bigint,
      outputTokens: number,
      cacheReadTokens: number,
      totalToolCalls: number,
      durationMs: number,
      errorEnvelope?: unknown
    ): void {
      const db = getDb()
      const stmt = db.prepare(
        "UPDATE events SET output_tokens = $out, cache_read_tokens = $cache, " +
        "total_tool_calls = $calls, duration_ms = $dur, error_envelope = $err " +
        "WHERE id = $id"
      )
      stmt.run({
        $id: id,
        $out: outputTokens,
        $cache: cacheReadTokens,
        $calls: totalToolCalls,
        $dur: durationMs,
        $err: errorEnvelope ? JSON.stringify(errorEnvelope) : null,
      })
    }

    // 테스트 helper — 다른 plan에서 사용 안 함
    export function _resetForTest(): void {
      if (_db) { _db.close(); _db = null }
    }
    ```

    `audit/log.test.ts` — RED→GREEN:
    1. getDb() 첫 호출 → data/ 디렉토리 생성 + Database 인스턴스 반환
    2. beginTurn("test", "model-x") → bigint id 반환 + events row 1 (parent_id NULL, prompt="test")
    3. beginTurn 2000자 trim: prompt = "a".repeat(3000) → DB의 prompt.length === 2000
    4. logTool(turnId, "tool", {a:1}, "ok", true, 50) → child row INSERT (parent_id === turnId, tool_name === "tool", ok === 1n)
    5. logTool 500자 trim: result_summary = "x".repeat(800) → DB의 result_summary.length === 500
    6. endTurn(turnId, 100, 50, 3, 1500) → UPDATE 후 SELECT으로 output_tokens === 100n 검증
    7. PITFALL #10 verify: `db.prepare("SELECT * FROM events WHERE id = $x")` 실행 시 `stmt.get({ wrong_key: 1 })` → expect.toThrow()

    **테스트 isolation:** beforeEach 마다 `_resetForTest()` + `unlinkSync('./data/copilot.db')` (existsSync check) + getDb() 재초기화. 테스트 격리 보장.
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && bun test audit/log.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `audit/log.ts` exists; `grep -c 'export function beginTurn' audit/log.ts` == 1
    - `grep -c 'export function logTool' audit/log.ts` == 1
    - `grep -c 'export function endTurn' audit/log.ts` == 1
    - `grep -c 'strict: true' audit/log.ts` >= 1 (PITFALL #10)
    - `grep -c 'safeIntegers: true' audit/log.ts` >= 1 (PITFALL #10)
    - `grep -c 'PRAGMA journal_mode = WAL' audit/log.ts` >= 1
    - `grep -c "slice(0, 2000)" audit/log.ts` >= 1 (D-14.3 prompt trim)
    - `grep -c "slice(0, 500)" audit/log.ts` >= 1 (D-14.3 result_summary trim)
    - `bun test audit/log.test.ts` exits 0 with 7+ tests pass
    - 테스트 후 `data/copilot.db` 파일 존재 (strict 모드 동작 확인)
  </acceptance_criteria>
  <done>audit/log.ts 3 함수 모두 RED→GREEN. PITFALL #10 strict mode 라이브 검증. D-14.3 minimum schema 영속화.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit/log.ts → bun:sqlite | INSERT/UPDATE 매개변수가 PITFALL #10 strict 모드에서 검증. 오타 binding 키 → throw. |
| state/services.yaml git tracked | data/copilot.db git ignored (D-14.2) — binary diff 무의미. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | binding key 오타 시 NULL 묵음 INSERT (PITFALL #10) | mitigate | `Database(..., { strict: true })` 모드 — 누락/오타 binding은 throw. Test 7 (PITFALL #10 verify)이 이를 명시 검증. |
| T-01-03-02 | Information Disclosure | prompt에 user secret 평문 저장 | mitigate (partial) | D-14.3 prompt 2000자 trim + result_summary 500자 trim. data/copilot.db는 .gitignore + 파일시스템 권한 (1인 사용 — 추가 암호화 미적용). |
| T-01-03-03 | Tampering | binary DB가 state/ git에 들어가 fast-forward only 위반 | mitigate | `.gitignore`에 `/data/` 등재 (D-14.2). state/는 git tracked (Phase 2 audit), data/는 ignored. |
| T-01-03-04 | DoS | result_summary 500자 cap 미적용 시 readLogs 큰 출력이 SQLite row 비대화 | mitigate | logTool()에서 `.slice(0, 500)` trim (D-14.3). Test 5가 검증. |
| T-01-03-05 | Repudiation (안전 측면) | turn 종료 시 endTurn 호출 누락 → output_tokens 등 NULL | accept | Phase 1 — agent/loop.ts (Plan 06)가 finally 블록에서 호출 보장. 호출 누락 시 NULL 컬럼은 cost monitoring에서 식별 가능 (오작동 신호). |
| T-01-03-06 | Information Disclosure | error_envelope에 secret 포함 가능 | mitigate | error_envelope = `{ problem, cause, fix, retryable }` shape (D-15.4). cause 필드는 _envelope.ts run()에서 200자 trim (Plan 02). |
</threat_model>

<verification>
- `bun test audit/log.test.ts` 7+ tests pass
- 테스트 후 `bunx sqlite3 data/copilot.db ".schema events"` 실행 시 hybrid schema 출력 (parent_id 컬럼 + 2 인덱스)
- `git status` → data/copilot.db는 untracked OR ignored (.gitignore /data/ 동작)
- `bun -e "import {beginTurn, logTool, endTurn} from './audit/log'; const id = beginTurn('hi','m'); logTool(id,'t',{a:1},'r',true,1); endTurn(id,10,5,1,100); console.log('OK')"` 출력 "OK"
</verification>

<success_criteria>
- AUDIT-01 충족: logTool()이 모든 read tool 호출 기록 (Plan 04에서 _envelope.ts wire 시 자동 적용)
- AUDIT-03 충족: beginTurn/endTurn이 timestamp/model/token usage/tool call 횟수 저장
- D-14.1 hybrid schema (turn + child tool) 영속화
- D-14.2 /data/ git ignore
- D-14.3 minimum schema (LOOP-07 idempotency_key 미포함 — Plan 06으로 deferred)
- D-14.4 모든 read tool 호출 기록 (Plan 04에서 _envelope.ts와 wire)
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-only-knowledge-layer/01-03-SUMMARY.md` with:
- audit/schema.sql 컬럼 list (table)
- audit/log.ts API signature 표 (beginTurn/logTool/endTurn)
- 테스트 결과 (7+ tests, PITFALL #10 검증 포함)
- 마찰 (FRICTION carry-over)
</output>
</content>
</invoke>