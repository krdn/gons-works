---
phase: 01-read-only-knowledge-layer
plan: 10
type: hotfix
wave: 5
depends_on: [01-06, 01-07]
files_modified:
  - agent/loop.ts
  - src/server.ts
  - agent/loop.test.ts
  - src/server.test.ts
  - .planning/phases/01-read-only-knowledge-layer/FRICTION.md
autonomous: true
gap_closure: true
requirements:
  - LOOP-01 (live wire)
  - LOOP-04 (orphan tool_use 방지)
  - LOOP-05 (history compaction)
  - READ-01 (RAG + 라이브 docker → SSE final)
  - READ-04 (drift event)
  - UI-04 (SSE final 클라이언트 도달)
tags:
  - phase-1
  - hotfix
  - carry-forward
  - d-e1-blocker

must_haves:
  truths:
    - "Phase 1 ROADMAP Success Criteria #1/#2/#3 라이브 환경에서 wire 결함 없이 PASS"
    - "agent/loop.ts two-step tool-use turn(2+ tool 호출 후 synthesis)이 정상 종료하여 final SSE event가 클라이언트에 도달"
    - "server.ts SSE wrapper가 iterate 종료 후 stream을 close하기 전에 모든 emit(특히 final)을 클라이언트에 flush"
    - "단일 turn 2-tool 시나리오에 대한 integration test 1개 신설하여 회귀 방지"
    - "D-E1 게이트(02-01-SMOKE-LOG)를 재실행하여 5/5 PASS 도달 가능"
  artifacts:
    - path: "agent/loop.ts"
      provides: "F-1 fix (two-step tool-use turn 400 invalid_request_error 해결)"
      contains: ""
    - path: "src/server.ts"
      provides: "F-2 fix (SSE final emit 클라이언트 도달 보장)"
      contains: ""
    - path: ".planning/phases/01-read-only-knowledge-layer/FRICTION.md"
      provides: "F-1/F-2 root cause + fix 기록 (Phase 2 FRICTION에서 carry-back)"
      contains: "F-1"
      min_lines: 20
  key_links:
    - from: "01-10-PLAN.md"
      to: ".planning/phases/02-propose-apply-approval-gate/FRICTION.md"
      via: "F-1/F-2 항목 cross-reference"
      pattern: "F-1"
    - from: "01-10-PLAN.md"
      to: ".planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md"
      via: "audit row id=8 + req_011Canaq7qP5ZbJXfECrMDt5 인용"
      pattern: "req_011"
---

<objective>
Phase 1 carry-forward hotfix. D-E1 게이트(`.planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md`)가 22/22 unit/integration PASS 코드에서 라이브 결함 2건을 lock-in. Phase 1 수준의 결함이므로 Phase 1 boundary로 되돌려 처리한다. F-3(Voyage 함정)은 코드 수정 없는 운영 SOP 항목이라 본 plan 범위에서 제외.

Purpose: Phase 2 진입을 차단하는 wire 결함 2건을 끝낸다. D-E1 게이트가 정상 동작하여 결함을 잡았으므로 게이트는 그대로 두고, hotfix 후 D-E1을 재실행해 5/5 PASS 도달.
Output:
  1. `agent/loop.ts` 수정 — two-step tool-use turn 400 해결
  2. `src/server.ts` 수정 — SSE final emit 클라이언트 도달 보장
  3. 회귀 방지 integration test 2개 추가
  4. Phase 1 FRICTION.md 갱신 (F-1/F-2 + fix)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md
@.planning/phases/01-read-only-knowledge-layer/FRICTION.md
@.planning/phases/02-propose-apply-approval-gate/FRICTION.md
@.planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md
@agent/loop.ts
@src/server.ts
@agent/sse.ts
@agent/loop.test.ts
@src/server.test.ts

<interfaces>
<!-- F-1 (HIGH): two-step tool-use turn 400 invalid_request_error -->
<!-- - audit DB events.id=5 (req_011CanZMFphsM333ojVTmqCs) + id=8 (req_011Canaq7qP5ZbJXfECrMDt5) -->
<!-- - prompt="ais-prod redis 어디 쓰여?" → listContainers + readLogs 후 두 번째 callWithFallback에서 400 -->
<!-- - Anthropic 응답: "messages: at least one message is required" -->
<!-- - 가설 우선순위: -->
<!--    H1 (HIGH): cli-proxy-api(http://192.168.0.5:8317)가 tool_use/tool_result block 변환 시 -->
<!--               messages를 빈 배열로 깎음 → 직접 console.anthropic.com 호출로 reproduce 시 PASS이면 H1 확정 -->
<!--    H2 (MED): agent/loop.ts L390-401 messages append 로직의 tool_result content 형식 -->
<!--               (JSON.stringify된 envelope vs Anthropic SDK 기대 shape) -->
<!--    H3 (LOW): compactHistory의 messages.length=3 path는 변경 없이 통과해야 하지만 -->
<!--               estimateTokens 폭주 → length 7+ 분기로 잘못 진입 가능성 -->

<!-- F-2 (HIGH): server.ts SSE final emit 클라이언트 미도달 (audit는 정상) -->
<!-- - hello probe (events id=11/12/13): error_envelope=null, output_tokens 285-339, duration 4-5초 → 정상 종료 -->
<!-- - 그러나 curl /chat-stream에는 drift event 후 stream 침묵 -->
<!-- - 가설 우선순위: -->
<!--    H4 (HIGH): src/server.ts L147 `emit: (ev) => { void emit(ev) }` fire-and-forget -->
<!--               iterate가 sync emit 콜백을 expect하므로 server.ts의 async writeSSE Promise가 -->
<!--               iterate resolve 후 streamSSE close 시 아직 resolve 안 된 경우 swallow -->
<!--    H5 (LOW): Hono streamSSE 헬퍼 자체의 flush 동작 (LOW — bun:test로 raw SSE assertion 검증) -->
-->

<!-- 회귀 방지 시나리오 -->
<!-- T1: agent/loop integration — 단일 turn에서 listContainers + readLogs(or readCompose) 2-tool 후 -->
<!--     synthesis text-delta + final emit이 정상 발화하는지. mock client로 결정적. -->
<!-- T2: server.ts integration — /chat-stream에 hello prompt 보내고 SSE 응답에서 -->
<!--     event 순서 drift → text-delta → final이 모두 도달하는지. -->
</interfaces>
</context>

<tasks>

<task type="diagnose">
  <name>Task 1: F-1 root cause 확정 — direct Anthropic call vs cli-proxy-api 비교</name>
  <files>(진단 단계 — 코드 수정 없음. 임시 spike script로 격리 검증)</files>
  <what-built>
    `tmp/diagnose-f1.ts` (gitignored) — agent/loop.ts의 messages 배열을 그대로 Anthropic API에 두 번 호출 (1차 텍스트 응답, 2차 tool_result 포함). primary(cli-proxy-api) + bypass(직접 console.anthropic.com) 양쪽 비교.
    출력: 양쪽 응답 status + body. 가설 H1/H2 확정 또는 reject.
  </what-built>
  <how-to-verify>
    1. `tmp/diagnose-f1.ts` 작성 (script만, 운영 코드 변경 안 함)
    2. `bun run tmp/diagnose-f1.ts` 실행
    3. 결과를 `.planning/phases/01-read-only-knowledge-layer/FRICTION.md`의 F-1 항목 root cause 라인에 한 줄로 기록 ("H1 확정" / "H2 확정" 등)
    4. 진단 후 spike 삭제 + .gitignore 확인
  </how-to-verify>
</task>

<task type="implement">
  <name>Task 2: F-1 fix — agent/loop.ts</name>
  <files>agent/loop.ts</files>
  <what-built>
    Task 1 진단 결과에 따라:
    - H1 확정 시: agent/loop.ts에 `// PITFALL: cli-proxy-api는 multi-turn tool_use/tool_result 흐름에서 messages 변환 결함 — fallback or direct path 강제` 코멘트 + env가 cli-proxy-api인 경우 자동으로 console.anthropic.com fallback 사용 (env에 ANTHROPIC_FALLBACK_API_KEY/_BASE_URL 설정 필요 — env.ts에서 미설정 시 친절 에러 포함)
    - H2 확정 시: tool_result block content 형식을 Anthropic SDK 정식 shape으로 수정 (필요 시 string 대신 array of content blocks 등)
    - H3 확정 시: compactHistory의 estimateTokens 분기 + messages.length 분기 둘 다 점검하여 빈 배열 방지

    공통: 다음 callWithFallback 직전에 `if (messages.length === 0) throw new Error("internal: empty messages — tool_result append 실패 (req_id=...)")` defense-in-depth 추가.
  </what-built>
  <how-to-verify>
    - `bun test agent/loop.test.ts` 단위 테스트 PASS 유지
    - 새 integration test (Task 4 T1) 추가 후 PASS
    - 기존 22/22 단위/통합 테스트 회귀 없음
  </how-to-verify>
</task>

<task type="implement">
  <name>Task 3: F-2 fix — src/server.ts SSE wrapper</name>
  <files>src/server.ts</files>
  <what-built>
    iterate가 sync emit 콜백을 expect하므로 server.ts에서:
    - emit 콜백 내부에서 `await stream.writeSSE(...)`의 결과를 모아 sync 큐로 처리
    - 또는 iterate에 async-aware emit interface 추가 (emit이 Promise를 반환하면 iterate가 await — 영향 범위 최소화 우선)
    - 또는 streamSSE 종료 직전 `await Promise.allSettled(pendingWrites)` 보장

    채택안: 가장 작은 수정 — server.ts L101-167에서 `pendingWrites: Promise<void>[]` 배열 유지, emit마다 push, finally에서 `await Promise.allSettled(pendingWrites)` 후 watchdog.cancel(). LOOP-06 watchdog 동작은 유지.

    핵심: iterate 내부 인터페이스 변경 없이 server.ts 단독 수정.
  </what-built>
  <how-to-verify>
    - `bun test src/server.test.ts` 단위 테스트 PASS 유지
    - 새 integration test (Task 4 T2) 추가 후 PASS
    - 라이브 smoke "hello" prompt가 SSE에 drift + text-delta + final 모두 emit
  </how-to-verify>
</task>

<task type="test">
  <name>Task 4: 회귀 방지 integration test 2개</name>
  <files>agent/loop.test.ts, src/server.test.ts</files>
  <what-built>
    T1 (agent/loop.test.ts): mock Anthropic client로 1) text+tool_use 응답 → 2) tool_use 응답 → 3) text 응답 시퀀스 setup. iterate 호출 후 emit log를 검증:
    - tool-start × 2, tool-result × 2, text-delta ≥ 2, final 정확히 1번
    - 두 번째 callWithFallback에 전달된 messages.length > 0

    T2 (src/server.test.ts): bun:test로 raw SSE 응답 파싱. mock iterate를 inject하여 text-delta + final emit 시퀀스 보장. 검증: SSE body에 `event: final\n` 라인이 정확히 1번 포함.
  </what-built>
  <how-to-verify>
    `bun test` 전체 통과 (22 + 2 = 24/24)
  </how-to-verify>
</task>

<task type="document">
  <name>Task 5: Phase 1 FRICTION.md 갱신 + Phase 2 FRICTION 상태 동기화</name>
  <files>.planning/phases/01-read-only-knowledge-layer/FRICTION.md, .planning/phases/02-propose-apply-approval-gate/FRICTION.md</files>
  <what-built>
    Phase 1 FRICTION.md에 신규 섹션:
    - F-1 (carry-back from Phase 2 D-E1): root cause 확정 + fix commit hash + integration test reference
    - F-2 (carry-back from Phase 2 D-E1): 동일 패턴

    Phase 2 FRICTION.md의 F-1/F-2 항목에 상태 라인 추가:
    - "Status: RESOLVED in Phase 1 plan 01-10 (commit ...)"
  </what-built>
  <how-to-verify>
    grep -E "F-1|F-2" 양쪽 파일 모두 hit + Status RESOLVED 라인 존재
  </how-to-verify>
</task>

</tasks>

<verification>
- agent/loop.ts + src/server.ts 수정 commit 존재
- bun test 24/24 PASS (기존 22 + 신규 T1/T2 2개)
- Phase 1 FRICTION.md에 F-1/F-2 carry-back 기록
- Phase 2 FRICTION.md F-1/F-2 항목에 RESOLVED 상태 라인
- 라이브 검증은 본 plan 범위 외 — D-E1 재실행(`/gsd-execute-phase 2 --plan 01` 또는 신규 SMOKE-LOG-2)은 별도 호출
</verification>

<success_criteria>
- [ ] Task 1 진단 결과가 FRICTION.md F-1 root cause 라인에 명시 (H1/H2/H3 중 하나 확정)
- [ ] agent/loop.ts 수정 + 단위 테스트 회귀 없음
- [ ] src/server.ts 수정 + 단위 테스트 회귀 없음
- [ ] integration test T1 + T2 신설 + PASS
- [ ] Phase 1 FRICTION.md + Phase 2 FRICTION.md 동기화
- [ ] 모든 변경에 atomic commit (Task 단위 1 commit씩 권장)
- [ ] D-E1 재검증을 위한 후속 호출 안내 라인이 SUMMARY에 포함
</success_criteria>

<output>
완료 후 `.planning/phases/01-read-only-knowledge-layer/01-10-SUMMARY.md` 작성:
- 진단 결론 (H1/H2/H3 중 어느 가설 확정)
- 적용된 fix 요약 (코드 변경 라인)
- 테스트 결과 (24/24 PASS)
- D-E1 재실행 안내 (`/gsd-execute-phase 2 --plan 01` 호출 → 02-01-SMOKE-LOG-2.md 작성 흐름)

git commit 컨벤션 (Task별 atomic):
- `fix(01): 10/T1 — F-1 root cause diagnose (Hx 확정)`
- `fix(01): 10/T2 — agent/loop.ts F-1 fix`
- `fix(01): 10/T3 — src/server.ts F-2 fix`
- `test(01): 10/T4 — F-1/F-2 회귀 방지 integration test`
- `docs(01): 10/T5 — FRICTION.md F-1/F-2 carry-back + Phase 2 동기화`
</output>
