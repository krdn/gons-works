---
phase: 01-read-only-knowledge-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/_envelope.ts
  - tools/_envelope.test.ts
  - tools/_index.ts
  - tools/_index.test.ts
autonomous: true
requirements:
  - LOOP-02
  - LOOP-04
  - LOOP-06
tags:
  - phase-1
  - tools
  - envelope
  - error-handling

must_haves:
  truths:
    - "모든 tool wrapper가 공통 ToolError envelope { problem, cause, fix, retryable } 반환 (LOOP-02)"
    - "tool이 throw해도 _envelope.run()이 ToolError를 반환하여 agent loop에서 항상 tool_result push 가능 (LOOP-04 orphan 방지)"
    - "30초 timeout이 AbortController로 관리되며 timeout 도달 시 envelope의 retryable=true로 반환 (LOOP-06)"
  artifacts:
    - path: "tools/_envelope.ts"
      provides: "run() generic wrapper + ToolError shape + isToolError type guard + AbortController 30s + audit hook"
      exports:
        - run
        - ToolError
        - isToolError
      contains: "AbortController"
      min_lines: 50
    - path: "tools/_index.ts"
      provides: "3 read tool input schema (Zod) + TOOL_SCHEMAS 배열 (Anthropic.Tool[]) + 입력 타입 export"
      exports:
        - TOOL_SCHEMAS
        - ListContainersInput
        - ReadLogsInput
        - ReadComposeInput
        - ListContainersArgs
        - ReadLogsArgs
        - ReadComposeArgs
      contains: "z.toJSONSchema"
  key_links:
    - from: "tools/_envelope.ts run()"
      to: "AbortController"
      via: "setTimeout + ac.abort()"
      pattern: "AbortController"
    - from: "tools/_index.ts TOOL_SCHEMAS"
      to: "Anthropic.Tool['input_schema']"
      via: "z.toJSONSchema(schema)"
      pattern: "z\\.toJSONSchema"
---

<objective>
모든 read tool이 공유할 공통 wrapper(`tools/_envelope.ts`)와 Anthropic API에 전달할 tool schema 배열(`tools/_index.ts`)을 정의한다. 이 plan의 산출물 없이는 Wave 2 read tools (Plan 04)와 Wave 3 agent loop (Plan 06) 모두 시작 불가.

Purpose: PITFALL #1 (orphan tool_use)와 PITFALL #5 (cost runaway)를 architectural level에서 차단. envelope shape 일관성이 system prompt의 contract base가 된다 (D-15.1).
Output: tools/_envelope.ts (TDD 패턴 with `run()` 함수), tools/_index.ts (3 read tool schema 배열).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md
@.planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@src/env.ts
@spikes/05-zod-schema.test.ts

<interfaces>
<!-- src/env.ts EnvProblem (PATTERNS.md `tools/_envelope.ts` 섹션 참조): -->
```typescript
export interface EnvProblem { problem: string; cause: string; fix: string }
```

<!-- ToolError 확장 shape (ARCHITECTURE.md Pattern 2 + PATTERNS.md tools/_envelope.ts): -->
```typescript
export interface ToolError {
  problem: string    // 무엇이 실패했는지
  cause: string      // 왜 (stack-safe excerpt)
  fix: string        // "retry" | "SSH 점검" | shell 명령 verbatim
  retryable: boolean
}
```

<!-- spikes/05-zod-schema.test.ts: z.toJSONSchema(schema) → Anthropic.Tool['input_schema'] -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools/_envelope.ts — ToolError shape + run() AbortController wrapper (LOOP-02 + LOOP-06 + D-15.3)</name>
  <files>tools/_envelope.ts, tools/_envelope.test.ts</files>
  <read_first>
    - src/env.ts (EnvProblem shape — PATTERNS.md analog)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `tools/_envelope.ts` 섹션 (line 244-315)
    - .planning/research/PITFALLS.md #1 (orphan tool_use) + #12 (SSH timeout)
    - .planning/phases/01-read-only-knowledge-layer/01-CONTEXT.md D-15.3 + D-15.4
    - .planning/research/ARCHITECTURE.md Pattern 2 (line 152-183)
  </read_first>
  <behavior>
    - happy path: `await run("listContainers", async (signal) => "ok")` returns "ok"
    - error path: tool throws Error("boom") → returns ToolError { problem: "tool listContainers 실패", cause: "boom", fix: "로그 확인 후 재시도", retryable: true }
    - timeout path: tool awaits 31s → AbortError caught → returns ToolError { problem: "tool listContainers timeout 30s", cause: "서버 도달·응답 지연", fix: "재시도 또는 SSH/docker context 상태 점검", retryable: true }
    - cause excerpt: error.message 길이 200자 초과 시 200자로 trim
    - isToolError: { problem: 'x', retryable: true } → true; "string" → false; { problem: 'x' } (no retryable) → false
    - never throws: catch 경로에서 절대 throw 금지 (PITFALL #1)
  </behavior>
  <action>
    `tools/_envelope.ts` 생성 — PATTERNS.md `tools/_envelope.ts` 섹션 그대로 채택 (D-15.3 lock):

    ```typescript
    export interface ToolError {
      problem: string
      cause: string
      fix: string
      retryable: boolean
    }

    export function isToolError(x: unknown): x is ToolError {
      return typeof x === "object" && x !== null
        && "problem" in x && "retryable" in x
        && typeof (x as ToolError).problem === "string"
        && typeof (x as ToolError).retryable === "boolean"
    }

    export async function run<T>(
      name: string,
      fn: (signal: AbortSignal) => Promise<T>,
      timeoutMs = 30_000
    ): Promise<T | ToolError> {
      const ac = new AbortController()
      const timer = setTimeout(() => ac.abort(), timeoutMs)
      try {
        return await fn(ac.signal)
      } catch (err) {
        // PITFALL #1: 절대 throw — 항상 ToolError 반환
        if ((err as Error).name === "AbortError") {
          return {
            problem: `tool ${name} timeout ${timeoutMs / 1000}s`,
            cause: "서버 도달·응답 지연",
            fix: "재시도 또는 SSH/docker context 상태 점검",
            retryable: true,
          }
        }
        return {
          problem: `tool ${name} 실패`,
          cause: ((err as Error).message ?? String(err)).slice(0, 200),
          fix: "로그 확인 후 재시도",
          retryable: true,
        }
      } finally {
        clearTimeout(timer)
      }
    }
    ```

    **AUDIT hook 자리표시자 (Plan 03 audit/log.ts 완성 후 Plan 04에서 wire — 이 plan에는 logTool import 안 함):**
    - 주석 한 줄 추가: `// AUDIT-01/D-14.4: Plan 03의 audit/log.ts logTool() 호출은 Plan 04 read tool wrapping 단계에서 추가. _envelope.ts 자체는 audit-agnostic 유지 (test simplicity).`

    `tools/_envelope.test.ts` — RED→GREEN:
    1. happy: `await run("t", async () => "ok")` → toBe("ok")
    2. error: throw "boom" → ToolError shape 5 필드 검증 (problem 포함 "tool t 실패", cause === "boom", retryable === true)
    3. timeout: `await new Promise(r => setTimeout(r, 50))` with timeoutMs=10 → ToolError shape (problem 포함 "timeout")
    4. isToolError: 3 케이스 (true / false-string / false-no-retryable)
    5. cause trim: throw new Error("a".repeat(300)) → cause.length === 200
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && bun test tools/_envelope.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/_envelope.ts` exists; `grep -c 'export interface ToolError' tools/_envelope.ts` >= 1
    - `grep -c 'AbortController' tools/_envelope.ts` >= 1 (LOOP-06 timeout)
    - `grep -c 'export function isToolError' tools/_envelope.ts` >= 1
    - `grep -c 'export async function run' tools/_envelope.ts` >= 1
    - `grep -c 'retryable: true' tools/_envelope.ts` >= 2 (timeout + generic catch)
    - `grep -c 'throw' tools/_envelope.ts` == 0 OR all `throw` are inside test file scope only — _envelope.ts run() never throws (PITFALL #1)
    - `bun test tools/_envelope.test.ts` exits 0 with 5+ tests pass
    - timeout 테스트가 30s 내에 완료 (timeoutMs을 작게 override한 test)
  </acceptance_criteria>
  <done>_envelope.ts ToolError 정의 + run() wrapper 구현. PITFALL #1/#12에 대한 architectural guard 완료. RED→GREEN 5+ tests.</done>
</task>

<task type="auto">
  <name>Task 2: tools/_index.ts — 3 read tool Zod input schema + TOOL_SCHEMAS 배열 (DESIGN.md correction #3)</name>
  <files>tools/_index.ts, tools/_index.test.ts</files>
  <read_first>
    - spikes/05-zod-schema.test.ts (z.toJSONSchema 패턴 — DESIGN.md correction #3 검증된 spike)
    - .planning/phases/01-read-only-knowledge-layer/01-PATTERNS.md `tools/_index.ts` 섹션 (line 318-365)
    - .planning/research/STACK.md (line 125-160 Zod v4 + z.toJSONSchema)
    - .planning/research/PITFALLS.md #6 prevention block (sanitization은 Plan 04 readLogs에서)
    - tools/_envelope.ts (Task 1 결과물 — type re-export 가능)
  </read_first>
  <action>
    `tools/_index.ts` 생성 — PATTERNS.md `tools/_index.ts` 섹션 그대로 채택:

    ```typescript
    import { z } from "zod"
    import type Anthropic from "@anthropic-ai/sdk"

    // listContainers — READ-01
    export const ListContainersInput = z.object({
      filter: z.object({
        state: z.enum(["running", "stopped", "all"]).default("running"),
        namePattern: z.string().optional(),
      }).optional(),
      limit: z.number().int().min(1).max(100).default(20),
    })

    // readLogs — READ-02 (lines max 1000, since/until ISO datetime)
    export const ReadLogsInput = z.object({
      containerName: z.string().min(1),
      lines: z.number().int().min(1).max(1000).default(200),
      since: z.string().optional(),
      until: z.string().optional(),
    })

    // readCompose — READ-03 (remote path on 192.168.0.5)
    export const ReadComposeInput = z.object({
      composePath: z.string().min(1),
    })

    // DESIGN.md correction #3: z.toJSONSchema (대문자 JSON) — Zod v4 내장
    // zod-to-json-schema 패키지 절대 사용 금지 (Zod v4와 호환 안 됨 — empty {} 반환)
    export const TOOL_SCHEMAS: Anthropic.Tool[] = [
      {
        name: "listContainers",
        description: "192.168.0.5 home-server 컨텍스트의 docker 컨테이너 목록을 가져온다. 5 핵심 stack(news/ais/n8n/open-webui/krdn-fx)의 라이브 상태 확인용.",
        input_schema: z.toJSONSchema(ListContainersInput) as Anthropic.Tool["input_schema"],
      },
      {
        name: "readLogs",
        description: "지정된 컨테이너의 최근 로그를 가져온다 (sanitized — SECRET/KEY/PASSWORD 라인 제거). lines 기본 200, 최대 1000. since/until로 시간 범위 지정 가능.",
        input_schema: z.toJSONSchema(ReadLogsInput) as Anthropic.Tool["input_schema"],
      },
      {
        name: "readCompose",
        description: "192.168.0.5의 docker-compose 파일 내용을 ssh로 읽어온다. composePath는 운영 서버의 절대 경로.",
        input_schema: z.toJSONSchema(ReadComposeInput) as Anthropic.Tool["input_schema"],
      },
    ]

    // handler에서 사용할 입력 타입
    export type ListContainersArgs = z.infer<typeof ListContainersInput>
    export type ReadLogsArgs = z.infer<typeof ReadLogsInput>
    export type ReadComposeArgs = z.infer<typeof ReadComposeInput>
    ```

    `tools/_index.test.ts` — sanity tests:
    1. TOOL_SCHEMAS.length === 3
    2. 모든 schema의 `input_schema.type === "object"` (Anthropic 호환)
    3. listContainers schema에 `filter`/`limit` properties 존재
    4. readLogs schema에 `containerName`/`lines` properties 존재 + `lines.maximum === 1000`
    5. ListContainersInput.safeParse({}) 성공 (filter/limit 모두 default)
    6. ReadLogsInput.safeParse({}) 실패 (containerName required)

    **prohibited:** `import { zodToJsonSchema } from "zod-to-json-schema"` — Zod v4 비호환 (DESIGN.md correction #3 explicit). PITFALLS.md FRICTION #9 grep false positive 회피 위해 `z.toJSONSchema` 표기 일관 (대문자 JSON).
  </action>
  <verify>
    <automated>cd /home/gon/projects/gon/gons-works && bun test tools/_index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/_index.ts` exists; `grep -c 'z.toJSONSchema' tools/_index.ts` >= 3 (3 read tool 모두)
    - `grep -c 'zod-to-json-schema' tools/_index.ts` == 0 (DESIGN.md correction #3 — 패키지 사용 금지)
    - `grep -c 'export const TOOL_SCHEMAS' tools/_index.ts` == 1
    - `grep -c 'export const ListContainersInput' tools/_index.ts` >= 1
    - `grep -c 'export const ReadLogsInput' tools/_index.ts` >= 1
    - `grep -c 'export const ReadComposeInput' tools/_index.ts` >= 1
    - `grep -E 'max\(1000\)' tools/_index.ts | wc -l` >= 1 (READ-02 line 최대 1000)
    - `bun test tools/_index.test.ts` exits 0 with 6+ tests pass
  </acceptance_criteria>
  <done>3 read tool Zod schema 정의 완료. TOOL_SCHEMAS 배열이 Anthropic.Tool[] 타입과 호환. Plan 06 agent/loop.ts가 import해서 Claude messages.create에 전달.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_use → tool input | Anthropic이 보낸 tool_use.input은 untrusted (LLM hallucination 가능). Zod parse가 첫 게이트. |
| tool execution → catch path | 모든 tool throw가 ToolError envelope로 변환 (PITFALL #1 orphan 방지). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | LLM이 readLogs.containerName에 shell metachar 주입 시도 (e.g., `"foo; rm -rf /"`) | mitigate | Zod schema (`ReadLogsInput.containerName: z.string().min(1)`)가 type 검증. 실제 shell 차단은 Plan 04 readLogs.ts가 `Bun.$ template literal`로 escape (FRICTION #9 grep false positive 주의). |
| T-01-02-02 | DoS | LLM이 readLogs.lines에 1000000 같은 큰 수 전달 | mitigate | `ReadLogsInput.lines: z.number().int().min(1).max(1000)`로 cap (READ-02 lock). |
| T-01-02-03 | Tampering | LLM이 readCompose.composePath에 `../../../../etc/passwd` 전달 | accept (Phase 1 read-only) | Phase 1은 read-only — readCompose는 SSH cat이지만 192.168.0.5 home-server 한 곳만 접근. Plan 04에서 추가 path validation 검토. |
| T-01-02-04 | Information Disclosure | tool error의 cause 필드에 stack trace + 파일 경로 노출 | mitigate | _envelope.ts run()에서 cause = `(err as Error).message.slice(0, 200)` — stack trace 미포함, 200자 cap. |
| T-01-02-05 | Tampering (orphan tool_use) | tool throw 시 _envelope.run()이 throw하면 PITFALL #1 발생 (conversation poison) | mitigate | run()의 catch 경로가 절대 throw 안 함 — 항상 ToolError 반환. Test가 명시적으로 `.toThrow()` 부재 검증 (Task 1 acceptance). |
</threat_model>

<verification>
- `bun test tools/_envelope.test.ts` 5+ tests pass (happy / error / timeout / isToolError / cause trim)
- `bun test tools/_index.test.ts` 6+ tests pass (TOOL_SCHEMAS shape / Zod parse 성공/실패)
- `bun -e "import { TOOL_SCHEMAS } from './tools/_index'; console.log(JSON.stringify(TOOL_SCHEMAS, null, 2))"` 실행 시 3개 tool의 valid Anthropic.Tool[] 출력
- `bun -e "import { run } from './tools/_envelope'; const r = await run('t', async () => 'ok'); console.log(r)"` 출력 "ok"
</verification>

<success_criteria>
- LOOP-02 충족: 모든 read tool이 _envelope.run()을 거쳐 ToolError envelope 반환
- LOOP-04 prerequisite: orphan 방지를 위한 catch-no-throw 보장 (Plan 06 agent/loop.ts에서 wire)
- LOOP-06 충족: 30s AbortController timeout — timeout 도달 시 envelope retryable=true
- TOOL_SCHEMAS가 Plan 06 agent/loop.ts의 messages.create에 그대로 전달 가능
</success_criteria>

<output>
After completion, create `.planning/phases/01-read-only-knowledge-layer/01-02-SUMMARY.md` with:
- _envelope.ts ToolError 필드 + 5 unit test 결과 요약
- _index.ts TOOL_SCHEMAS 3개 시그니처
- Zod v4 + z.toJSONSchema 호환성 라이브 검증 (DESIGN.md correction #3)
- 마찰 (FRICTION carry-over) — 있으면 .planning/phases/01-*/FRICTION.md 갱신
</output>
</content>
</invoke>