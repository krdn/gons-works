---
phase: 02-propose-apply-approval-gate
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - state/commit.ts
  - state/commit.test.ts
  - state/.git/hooks/pre-commit
  - state/.gitignore
autonomous: true
requirements:
  - APPLY-06
  - APPLY-07
tags:
  - phase-2
  - state-commit
  - sanitization
  - fast-forward-only
  - pitfall-8-prevention

must_haves:
  truths:
    - "buildMessage(action)이 D-D1 양식(`apply(<stack>): <command> [<summary>]` Subject + 4 body 필드)을 결정적으로 생성"
    - "sanitize(text, cap)가 newline/backtick/dollar/null 제거 후 cap 적용 (D-D3 시퀀스)"
    - "commitWithMessage()가 Bun.spawn(['git','commit','-F',tmpFile,'--']) 임시파일 경유로 셸 통과 회피 (Bun.$ 사용 금지)"
    - "state/.git/hooks/pre-commit이 fast-forward only을 enforce (rebase/rewrite 감지 시 exit 1, APPLY-07 + PITFALL #8)"
    - "state/.gitignore에 /.pending/ 추가 (marker 디렉토리는 git에 포함 안 함, D-C1)"
  artifacts:
    - path: "state/commit.ts"
      provides: "buildMessage / sanitize / commitWithMessage 3 export + ApplyResult 타입"
      exports: ["buildMessage", "sanitize", "commitWithMessage"]
      contains: "User-Prompt"
    - path: "state/commit.test.ts"
      provides: "buildMessage grep 가능 검증 + sanitize 시퀀스 검증 + commitWithMessage roundtrip (state/ 격리 임시 git repo)"
      contains: "AI-Reasoning"
    - path: "state/.git/hooks/pre-commit"
      provides: "POSIX shell hook (fast-forward only enforce)"
      contains: "rebase"
    - path: "state/.gitignore"
      provides: "/.pending/ 라인 추가"
      contains: "/.pending/"
  key_links:
    - from: "state/commit.ts commitWithMessage"
      to: "data/.tmp/state-msg-{nonce}.txt 임시파일"
      via: "Bun.spawn -F + -- 분리자 (D-D3 lock)"
      pattern: "spawn.*-F"
    - from: "state/.git/hooks/pre-commit"
      to: "git reflog -1 --format=%gs"
      via: "rebase|rewrite 감지 시 exit 1"
      pattern: "rebase"
    - from: "state/commit.ts buildMessage"
      to: ".planning/research/PITFALLS.md Pitfall 8"
      via: "fast-forward only commit msg shape이 hook 호환"
      pattern: "Nonce:"
---

<objective>
state/ git commit 유틸리티 + pre-commit hook + .gitignore. applyPatch가 의존하는 commit message 빌더(D-D1)와 sanitization(D-D3), 그리고 state/ 저장소의 fast-forward only enforcement(APPLY-07) 토대를 한 plan에 모아 둔다.

Purpose: APPLY-06 / APPLY-07 lock + PITFALL #8 prevention. 02-06 applyPatch가 호출 직전 단계로 의존. Wave 1 병렬 가능 (다른 plan과 file 충돌 없음 — state/ 디렉토리는 02-04 init script와 file 다름).
Output: `state/commit.ts` + 테스트 + `state/.git/hooks/pre-commit` shell + `state/.gitignore` 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-propose-apply-approval-gate/02-CONTEXT.md
@.planning/phases/02-propose-apply-approval-gate/02-PATTERNS.md
@.planning/research/PITFALLS.md
@audit/log.ts
@spikes/04-git-commit.test.ts

<interfaces>
```typescript
// state/commit.ts — 본 plan에서 작성

export interface ApplyResult {
  stack: "news" | "ais" | "n8n" | "open-webui" | "krdn-fx"
  command: string                  // D-B1 7-union의 raw 문자열 ("compose restart" 등)
  service: string                  // <svc>; 'compose ps'일 때 빈 문자열 OK
  fileEditSummary?: string         // "image bump 1.2->1.3" 또는 hunks={N} files={N} 자동 생성
  userPrompt: string               // 사용자 NL 입력 원본 (200자 cap 전)
  reasoning: string                // proposePatch.reasoning (500자 cap 전)
  nonce: string                    // crypto.randomUUID
  diffSummaryRaw?: string          // jsdiff hunks 카운트 등 — 자동 생성
}

// D-D1 lock — Subject 한 줄 + body 4 필드
export function buildMessage(action: ApplyResult): string

// D-D3 lock — newline/backtick/dollar/null 제거 후 cap
export function sanitize(text: string, cap: number): string

// D-D3 lock — Bun.spawn -F 임시파일, -- 분리자, cwd: "state"
// 실패 시 throw Error("git commit 실패 (exit X): {stderr 200자}")
export async function commitWithMessage(message: string, nonce: string): Promise<void>
```

<!-- D-D1 정확한 양식: -->
<!-- ``` -->
<!-- apply(<stack>): <command>[ [<file-edit-summary>]] -->
<!-- (빈 줄) -->
<!-- User-Prompt: <sanitize(userPrompt, 200)> -->
<!-- AI-Reasoning: <sanitize(reasoning, 500)> -->
<!-- Diff-Summary: <sanitize(fileEditSummary ?? diffSummaryRaw ?? "no-file-change", 100)> -->
<!-- Nonce: <nonce> -->
<!-- ``` -->

<!-- D-D3 sanitize sequence: -->
<!-- 1) /[\n\r]+/g → " / " -->
<!-- 2) /[`$\0]/g → "" -->
<!-- 3) .slice(0, cap) -->

<!-- 임시파일 경로: data/.tmp/state-msg-{nonce}.txt (data/는 .gitignore 자동 적용) -->
<!-- finally에서 unlinkSync (실패 시 swallow) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state/commit.ts 작성 (buildMessage + sanitize + commitWithMessage)</name>
  <files>state/commit.ts</files>
  <behavior>
    - buildMessage: 입력 ApplyResult로 Subject + body 4 필드 한 문자열 생성. fileEditSummary 없으면 Subject에 ` [...]` 부분 생략, body Diff-Summary는 "no-file-change"로 fallback.
    - sanitize: 빈 문자열 / 한국어 / shell metachar / multi-line 모두 1단계로 처리. cap=0이면 빈 문자열.
    - commitWithMessage: data/.tmp/state-msg-{nonce}.txt write → Bun.spawn(['git','commit','-F',tmpFile,'--']) cwd:"state" → exit≠0이면 stderr 200자 슬라이스로 throw → finally에서 unlink.
    - data/.tmp/ 디렉토리는 commitWithMessage가 mkdirSync({recursive:true})로 보장.
  </behavior>
  <action>
    `state/commit.ts` 작성:

    ```typescript
    import { writeFileSync, unlinkSync, mkdirSync, existsSync } from "node:fs"

    // 타입 export (interfaces 블록 참조)

    export function sanitize(text: string, cap: number): string {
      return String(text)
        .replace(/[\n\r]+/g, " / ")
        .replace(/[`$\0]/g, "")
        .slice(0, cap)
    }

    export function buildMessage(action: ApplyResult): string {
      const subjectExtra = action.fileEditSummary ? ` [${sanitize(action.fileEditSummary, 80)}]` : ""
      const subject = `apply(${action.stack}): ${action.command}${action.service ? " " + action.service : ""}${subjectExtra}`
      const body = [
        `User-Prompt: ${sanitize(action.userPrompt, 200)}`,
        `AI-Reasoning: ${sanitize(action.reasoning, 500)}`,
        `Diff-Summary: ${sanitize(action.fileEditSummary ?? action.diffSummaryRaw ?? "no-file-change", 100)}`,
        `Nonce: ${action.nonce}`,
      ].join("\n")
      return `${subject}\n\n${body}`
    }

    export async function commitWithMessage(message: string, nonce: string): Promise<void> {
      const tmpDir = "data/.tmp"
      if (!existsSync(tmpDir)) mkdirSync(tmpDir, { recursive: true })
      const tmpFile = `${tmpDir}/state-msg-${nonce}.txt`
      writeFileSync(tmpFile, message, { encoding: "utf-8" })
      try {
        const proc = Bun.spawn(
          ["git", "commit", "-F", tmpFile, "--"],
          { cwd: "state", stdout: "pipe", stderr: "pipe" },
        )
        const exit = await proc.exited
        if (exit !== 0) {
          const stderr = await new Response(proc.stderr).text()
          throw new Error(`git commit 실패 (exit ${exit}): ${stderr.slice(0, 200)}`)
        }
      } finally {
        try { unlinkSync(tmpFile) } catch { /* swallow */ }
      }
    }
    ```

    주의:
    - `Bun.$` 절대 사용 금지 (D-D3 lock — multi-line 한국어 + shell metachar 위험)
    - `--` 분리자는 args 배열의 마지막 요소
    - cwd:"state" — `state/` 디렉토리에서 git commit 실행 (state/ 자체가 git repo, D-04 carry-forward)
  </action>
  <verify>
    <automated>bun test state/commit.test.ts</automated>
  </verify>
  <done>state/commit.ts 3 export 컴파일 통과 + Task 2 테스트 PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: state/commit.test.ts — buildMessage grep + sanitize 시퀀스 + commitWithMessage roundtrip</name>
  <files>state/commit.test.ts</files>
  <behavior>
    1. **buildMessage Subject grep:** `apply(news): compose restart news-prod-app` (no file edit) — `^apply\\(news\\): compose restart` 매칭
    2. **buildMessage with fileEditSummary:** `apply(news): compose up -d news-prod-app [image bump 1.2->1.3]` 매칭
    3. **buildMessage body 4 필드:** User-Prompt / AI-Reasoning / Diff-Summary / Nonce 라인 각 1개 존재
    4. **sanitize newline:** `"a\nb\rc"` → `"a / b / c"` (cap 100)
    5. **sanitize backtick/dollar/null:** ``"`a$b c"`` → `"abc"`
    6. **sanitize cap:** 한국어 50자 + cap 20 → 20자 (Buffer 아니라 character 기준)
    7. **buildMessage cap 적용:** userPrompt 300자 → User-Prompt 라인은 200자
    8. **commitWithMessage roundtrip:** 임시 git repo (`mkdtemp` + `git init`) cwd 변경 → buildMessage → commitWithMessage → `git log -1 --format=%B` 출력이 입력 message와 일치 (한국어 multi-line 포함)
    9. **commitWithMessage 실패 분기:** 임시 git repo에서 `git commit` 직전 staging 비우면 (또는 git config user.email 미설정) exit≠0 → throw + 임시파일 unlink 확인
    10. **임시파일 unlink:** Task 8 성공 후 `data/.tmp/state-msg-*.txt` 미존재 확인 (또는 process tmp 디렉토리)
  </behavior>
  <action>
    Phase 1 `spikes/04-git-commit.test.ts:25-31` 패턴 참고하되 D-D3 spawn -F로 supersede.

    `mkdtempSync` 임시 git repo + `git init` + `git config user.email/name` (Phase 1 spike 04 참조) → commitWithMessage 호출 시 cwd override 위해 Bun.spawn에 외부에서 cwd 전달이 필요. **plan 결정**: commitWithMessage는 `cwd: "state"` 하드코딩이지만 test는 `process.chdir(tmpRepo + "/..")` 후 state 디렉토리 mock. 실용적 해결: commitWithMessage에 optional 두 번째 인자 cwd를 추가하여 test에서 override (production code는 default `"state"` 그대로).

    혹은 더 단순한 방법: test에서 `cwd` 인자를 추가하는 대신, monkey-patch 없이 process.chdir로 cwd를 임시 git repo로 옮기고 commitWithMessage에 cwd:"."를 통과하도록 인자 추가:
    ```typescript
    export async function commitWithMessage(message: string, nonce: string, cwd: string = "state"): Promise<void>
    ```

    test:
    ```typescript
    import { describe, test, expect, beforeEach, afterEach } from "bun:test"
    import { mkdtempSync, rmSync } from "node:fs"
    import { tmpdir } from "node:os"
    import { join } from "node:path"
    import { buildMessage, sanitize, commitWithMessage } from "./commit"

    describe("state/commit", () => {
      describe("sanitize", () => {
        test("newline 제거", () => { expect(sanitize("a\nb\rc", 100)).toBe("a / b / c") })
        test("backtick/dollar/null 제거", () => { expect(sanitize("`a$b c", 100)).toBe("abc") })
        test("cap 적용 (한국어)", () => { expect(sanitize("가".repeat(50), 20).length).toBe(20) })
      })

      describe("buildMessage", () => {
        const base = { stack: "news" as const, command: "compose restart", service: "news-prod-app", userPrompt: "재시작", reasoning: "테스트 사유", nonce: "abc1234" }
        test("Subject (no fileEdit)", () => {
          const msg = buildMessage(base)
          expect(msg).toMatch(/^apply\(news\): compose restart news-prod-app/)
        })
        test("Subject with summary", () => {
          const msg = buildMessage({ ...base, fileEditSummary: "image bump" })
          expect(msg).toMatch(/^apply\(news\): compose restart news-prod-app \[image bump\]/)
        })
        test("body 4 필드", () => {
          const msg = buildMessage(base)
          expect(msg).toContain("\nUser-Prompt: 재시작")
          expect(msg).toContain("\nAI-Reasoning: 테스트 사유")
          expect(msg).toContain("\nDiff-Summary: no-file-change")
          expect(msg).toContain("\nNonce: abc1234")
        })
        test("cap 적용 (userPrompt 300자)", () => {
          const longPrompt = "a".repeat(300)
          const msg = buildMessage({ ...base, userPrompt: longPrompt })
          const promptLine = msg.split("\n").find(l => l.startsWith("User-Prompt: "))!
          expect(promptLine.length).toBe("User-Prompt: ".length + 200)
        })
      })

      describe("commitWithMessage roundtrip", () => {
        let tmp: string
        beforeEach(async () => {
          tmp = mkdtempSync(join(tmpdir(), "gons-state-"))
          await Bun.spawn(["git", "init"], { cwd: tmp }).exited
          await Bun.spawn(["git", "config", "user.email", "t@t"], { cwd: tmp }).exited
          await Bun.spawn(["git", "config", "user.name", "t"], { cwd: tmp }).exited
          await Bun.write(join(tmp, "f.txt"), "x")
          await Bun.spawn(["git", "add", "."], { cwd: tmp }).exited
        })
        afterEach(() => rmSync(tmp, { recursive: true, force: true }))

        test("multi-line 한국어 commit body roundtrip", async () => {
          const action = { stack: "news" as const, command: "compose restart", service: "news-prod-app", userPrompt: "한국어\n줄바꿈", reasoning: "test `dangerous` $injection", nonce: crypto.randomUUID() }
          const msg = buildMessage(action)
          await commitWithMessage(msg, action.nonce, tmp)
          const proc = Bun.spawn(["git", "log", "-1", "--format=%B"], { cwd: tmp, stdout: "pipe" })
          await proc.exited
          const out = await new Response(proc.stdout).text()
          expect(out.trim()).toBe(msg.trim())
        })

        test("exit≠0 시 throw + 임시파일 cleanup", async () => {
          // staged 된 게 있음 → 일반 commit 성공. 일부러 실패시키려면 git config user.email 제거 후 호출.
          await Bun.spawn(["git", "config", "--unset", "user.email"], { cwd: tmp }).exited
          await expect(commitWithMessage("x", "fail-nonce", tmp)).rejects.toThrow(/git commit 실패/)
          // 임시파일 unlink 확인
          const tmpFile = "data/.tmp/state-msg-fail-nonce.txt"
          expect(existsSync(tmpFile)).toBe(false)
        })
      })
    })
    ```

    주의: `commitWithMessage`에 cwd 인자 추가 — production 호출자는 `cwd:"state"` default 사용.
  </action>
  <verify>
    <automated>bun test state/commit.test.ts</automated>
  </verify>
  <done>10개 이상 test PASS, fail 0.</done>
</task>

<task type="auto">
  <name>Task 3: state/.git/hooks/pre-commit shell hook 작성 + chmod +x</name>
  <files>state/.git/hooks/pre-commit</files>
  <action>
    POSIX shell 스크립트로 작성. `state/` git repo는 Phase 0에서 init됨 (D-04). 따라서 `state/.git/`이 이미 존재.

    파일 내용:
    ```sh
    #!/bin/sh
    # state/.git/hooks/pre-commit
    # APPLY-07 lock: state/ git은 fast-forward only.
    # PITFALL #8 prevention: rebase / history rewrite 차단.

    set -e

    LAST_REFLOG=$(git reflog -1 --format="%gs" 2>/dev/null || echo "")
    case "$LAST_REFLOG" in
      rebase*|*rewrite*|reset:*hard*)
        echo "[pre-commit] APPLY-07 violation: rebase/rewrite/reset --hard 감지. fast-forward only."
        exit 1
        ;;
    esac

    exit 0
    ```

    `chmod +x state/.git/hooks/pre-commit` 수행.

    **검증 시나리오 (manual, 본 plan에서 bun test 미적용 — shell):**
    ```bash
    cd state
    git commit --allow-empty -m "test ff" -F /dev/null  # exit 0
    git reset --hard HEAD~1 || true                       # 이전 commit 남는지 확인
    git commit --allow-empty -m "test ff after reset"    # exit 1 (block)
    cd ..
    ```
    실제 02-10 verify plan에서 이 시나리오 자동화.
  </action>
  <verify>
    <automated>test -x state/.git/hooks/pre-commit && grep -c "rebase" state/.git/hooks/pre-commit</automated>
  </verify>
  <done>파일 존재 + 실행 권한 + rebase 패턴 매칭.</done>
</task>

<task type="auto">
  <name>Task 4: state/.gitignore에 /.pending/ 추가</name>
  <files>state/.gitignore</files>
  <action>
    `state/.gitignore` 파일 — 없으면 신설, 있으면 끝에 라인 추가:

    ```
    /.pending/
    ```

    state/.pending/ 디렉토리는 02-06 applyPatch가 `mkdir -p`로 신설. marker 파일은 git에 포함하지 않음 (D-C1 lock — runtime state).

    **검증:** `cd state && git check-ignore .pending/test.json` → 매칭 출력 (가능 시 .pending/test.json mock 후 확인).
  </action>
  <verify>
    <automated>grep -c "^/.pending/$" state/.gitignore</automated>
  </verify>
  <done>state/.gitignore에 `/.pending/` 라인 1개 존재.</done>
</task>

</tasks>

<verification>
- `bun test state/commit.test.ts` PASS
- `test -x state/.git/hooks/pre-commit` (실행 권한)
- `grep "rebase" state/.git/hooks/pre-commit` 매칭
- `grep "^/.pending/$" state/.gitignore` 매칭
- `grep -c "Bun.\\\$" state/commit.ts` = 0 (Bun.$ 사용 금지 — D-D3 supersede)
- `grep -c "spawn.*-F" state/commit.ts` ≥ 1
- D-D1 양식 grep: `grep -E "apply\\(.*\\): " state/commit.test.ts` 매칭
</verification>

<success_criteria>
- [ ] state/commit.ts 3 export + 컴파일 통과
- [ ] state/commit.test.ts 10+ test PASS
- [ ] state/.git/hooks/pre-commit 실행 권한 + APPLY-07 enforce 로직
- [ ] state/.gitignore에 /.pending/ 추가
- [ ] APPLY-06 (commit body 4 필드 + sanitization) 만족
- [ ] APPLY-07 (fast-forward only) 만족
- [ ] D-D1 / D-D3 결정 lock 적용
- [ ] Bun.$ 사용 0회 (spawn -F + -- 분리자만)
</success_criteria>

<output>
완료 후 manual git commit:
`feat(02): 03 — state/commit.ts + pre-commit hook (APPLY-06/07, D-D1/D3, PITFALL #8)`

`.planning/phases/02-propose-apply-approval-gate/02-03-SUMMARY.md` 작성.

⚠ 주의: state/.git/hooks/pre-commit은 부모 repo의 git에서 추적 안 됨(.git 디렉토리 자체가 .gitignore). state/는 별도 git repo(D-04)이므로, hook 자체가 자동 백업이 안 된다. 02-04 init script에서 hook 작성을 idempotent하게 재현하거나, 본 plan에서 hook 작성 절차를 SUMMARY.md에 명시(향후 clone 시 재실행 필요).
</output>
