---
phase: 02-propose-apply-approval-gate
plan: 10
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
  - 02-06
  - 02-07
  - 02-08
  - 02-09
files_modified:
  - .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md
  - .planning/phases/02-propose-apply-approval-gate/FRICTION.md
autonomous: false
requirements:
  - AUDIT-02
  - DOG-03

tags:
  - phase-2
  - verification
  - e2e
  - dogfood
  - friction-meta

must_haves:
  truths:
    - "ROADMAP Phase 2 Success Criteria 5개 모두 D-A3 test compose stack 위에서 라이브 검증됨"
    - "AUDIT-02 grep — `git log state/`만으로 user prompt + AI reasoning 추적 가능 확인 (D-D1 양식 검색)"
    - "crash window simulation (kill -9 docker exec 직후) 후 재기동 → drift event 표시 + a/b/c 명령 안내 확인 (D-C2)"
    - "git commit fail simulation (pre-commit hook 일시 fail) → D-B3 역 docker rollback 시도 + envelope CRITICAL 발화 확인"
    - "/ship 명령으로 GitHub PR 생성 흐름이 매끄러움 — DOG-03 검증"
    - "FRICTION.md (Phase 2)에 dogfood 마찰 누적 — 5-key UI 진화, gsd-tools.cjs 부재, /ship 흐름, COPILOT_MODEL 일괄 swap 비용 등 (DOG-02)"
  artifacts:
    - path: ".planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md"
      provides: "ROADMAP Phase 2 5 Success Criteria 라이브 검증 표 + AUDIT-02 grep 증거 + crash/git-fail simulation 결과"
      contains: "Success Criteria"
      min_lines: 100
    - path: ".planning/phases/02-propose-apply-approval-gate/FRICTION.md"
      provides: "Phase 2 dogfood 마찰 (Phase 0/1 패턴 따라)"
      contains: "마찰"
  key_links:
    - from: "02-VERIFICATION.md"
      to: "ROADMAP Phase 2 5 Success Criteria"
      via: "각 Criteria마다 PASS/FAIL + 라이브 증거 (test stack 위)"
      pattern: "Criteria"
    - from: "02-VERIFICATION.md AUDIT-02 grep"
      to: "git log state/ --grep '^apply\\(' --pretty=fuller"
      via: "D-D1 양식 검증"
      pattern: "apply"
    - from: "FRICTION.md (Phase 2)"
      to: "~/.claude/plans/gstack-gsd-melodic-raven.md"
      via: "DOG-02 dogfood meta input"
      pattern: "DOG-02"
---

<objective>
Phase 2 출하 게이트. ROADMAP Phase 2 Success Criteria 5개 라이브 검증 + AUDIT-02 git log grep 검증 + crash/git-fail simulation + DOG-03 `/ship` 매끄러움 + FRICTION 누적. **모든 라이브 단계는 D-A3 test compose stack(192.168.0.8 alpine 더미) 위에서만 수행 — PROD 절대 조작 금지.**

Purpose: AUDIT-02 / DOG-03 lock + 출하 게이트. 02-09까지 모든 코드 land된 후 본 plan에서 단일 사용자 흐름으로 검증.
Output: 02-VERIFICATION.md (검증 표 + 증거) + FRICTION.md (Phase 2 마찰).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-propose-apply-approval-gate/02-CONTEXT.md
@.planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md
@.planning/phases/01-read-only-knowledge-layer/FRICTION.md
@.planning/phases/00-bootstrap-spikes/FRICTION.md
@tests/fixtures/test-compose.yml
@tests/setup/docker-context.ts

<interfaces>
<!-- ROADMAP Phase 2 5 Success Criteria: -->
<!-- 1. proposePatch 실행 시 unified diff 표시 + 5-key 인라인 legend 항상 visible -->
<!-- 2. y → docker exec 먼저 → 성공 후에만 state/ git commit (2PC 순서) -->
<!-- 3. docker 적용 실패 시 git commit 안 됨 + 이전 상태로 롤백 -->
<!-- 4. git log state/ body에 user prompt + AI reasoning 포함 -->
<!-- 5. /ship 실행으로 GitHub PR 생성 (DOG-03) -->

<!-- 추가 검증 시나리오 (CONTEXT specifics): -->
<!-- - crash window: docker exec 직후 kill -9 → 재기동 → marker → drift event a/b/c -->
<!-- - git commit fail: pre-commit hook 일시 fail → D-B3 역 docker 시도 envelope -->
-->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-A3 test stack 위에서 ROADMAP Phase 2 5 SC 라이브 검증</name>
  <files>.planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md</files>
  <what-built>
    이 task는 운영자가 manual로 라이브 시스템에서 5 SC를 검증하고 결과를 02-VERIFICATION.md에 기록한다.
    Claude는 검증 sequence를 명시하고 결과 표 템플릿을 작성한다 — 라이브 동작 자체는 운영자 책임.
  </what-built>
  <how-to-verify>
    ### 사전 준비
    1. `unset ANTHROPIC_API_KEY` (FRICTION #8 carry)
    2. test compose stack 기동:
       ```bash
       DOCKER_CONTEXT=default docker compose -f tests/fixtures/test-compose.yml up -d
       docker --context default ps --filter name=gons-test-
       # 2 컨테이너 확인 (gons-test-svc-a, gons-test-svc-b)
       ```
    3. 임시로 services.yaml에 test stack 등록 — 또는 더 단순화: 본 plan에서는 D-B1 union literal에 test stack을 넣지 않으므로 검증은 5 stack 중 1개를 선택해서 사용 (단, 그 stack의 1 컨테이너에 한해, **명령 효과 없는 것만 사용** — 예: `compose ps`, 또는 docker context를 default로 둔 채 5 stack 명령은 실패하므로 신중)

    **결정 lock (본 plan):** 검증 시나리오를 D-A3 test stack에 직접 매핑하기 위해, **services.yaml의 stack 이름을 임시로 'news' → test stack으로 매핑하지 않고**, 대신 **테스트 전용 분기**를 본 plan 내 임시 코드 수정으로 처리:
    - tools/applyPatch.ts의 REMOTE_COMPOSE_PATH (또는 agent/loop.ts에서 주입하는 매핑)에서 'news' 항목을 임시로 `tests/fixtures/test-compose.yml`로 교체 + SSH host를 'localhost' 또는 192.168.0.8 (본인) — **단 SSH 자체는 D-A5 lock**이므로 실용 우회: applyPatch가 test 모드일 때 SSH를 skip하고 로컬 docker compose 사용하는 분기를 environ flag로 추가.
    - 또는 더 단순: 검증을 unit + manual mock으로 한정하고 라이브 docker exec는 D-A3 test stack에 한해 사전 준비된 작은 wrapper script로 수행. 5 SC 중 #1/#4/#5는 mock + screenshot으로 검증 가능.

    **간단한 결정**: **검증을 두 갈래로 분리**:
    - **A. UI/audit 검증 (mock LLM):** SC #1/#4 — proposePatch tool 직접 호출 + UI 시각 확인 + git log grep
    - **B. 2PC 라이브 검증 (test stack):** SC #2/#3 — 라이브 docker exec on test stack (DOCKER_CONTEXT=default)
    - **C. /ship 흐름 (PR):** SC #5

    위 분리를 02-VERIFICATION.md에 명시.

    ### Step A — SC #1 (UI 5-key 인라인 legend) + SC #4 (git log body)
    1. server start: `bun run src/server.ts`
    2. 브라우저 접속, 다음 NL 입력: "test-svc-a를 재시작해줘 (test stack용)"
       또는 mock 호출로 proposePatch 직접 dispatch (curl):
       ```bash
       curl -N -H "x-session-id: t1" "http://127.0.0.1:PORT/chat-stream?prompt=news+stack+news-prod-app+재시작+(테스트)"
       ```
    3. 브라우저에서 approval-required 카드 시각 확인:
       - [ ] 인라인 legend `y=apply n=reject e=edit d=diff toggle a=abort` visible
       - [ ] diff `<pre>` 영역 표시
       - [ ] reasoning 영역 표시
    4. `y` 누름 → applied 카드 + git log 확인 (test stack 흐름이라면, 또는 PROD에 가까운 stack은 mock 처리):
       ```bash
       cd state && git log -1 --format=fuller --grep "^apply("
       # Expected:
       # apply(news): compose restart news-prod-app
       # 
       # User-Prompt: news stack ...
       # AI-Reasoning: ...
       # Diff-Summary: ...
       # Nonce: ...
       ```
    5. 결과를 02-VERIFICATION.md SC #1/#4 PASS 행에 기록

    ### Step B — SC #2 (2PC 순서) + SC #3 (rollback)
    1. test compose stack 위에서 happy path:
       - tools/applyPatch.ts에 REMOTE_COMPOSE_PATH override env (예: `APPLY_TEST_MODE=1`) 추가하여 'news' → tests/fixtures/test-compose.yml + SSH skip + local docker compose 사용
       - 또는 더 안전: 본 plan에 test 전용 별도 entrypoint 추가 (`scripts/test-apply-patch.ts`)로 applyPatch 함수 직접 호출 + DOCKER_CONTEXT=default
       ```bash
       APPLY_TEST_MODE=1 bun run scripts/test-apply-patch.ts
       # internally: applyPatch({proposal: {stack:"news", command:"compose restart", service:"test-svc-a", ...}, decision: {type:"approved"}, ...})
       # docker compose restart test-svc-a 실행 → state/.git에 commit 기록
       ```
    2. 2PC 순서 검증 (SC #2):
       - docker compose 실행 timestamp vs git commit timestamp 비교
       - `docker --context default events --since '5m ago' --until 'now' --format json | grep test-svc-a` (restart event 시각)
       - `cd state && git log -1 --format='%ai'` (commit 시각)
       - docker event 시각 < git commit 시각 = PASS

    3. rollback 검증 (SC #3):
       - 의도적 docker fail 트리거: `service: "non-existent-svc"` 입력
       - 결과: applyPatch outcome=rolled-back + git log에 commit 추가 안 됨 + 마커 삭제
       - `cd state && git log --since '1m ago' --oneline | wc -l` = 0 → PASS

    4. crash window simulation (CONTEXT specifics 추가 시나리오):
       - applyPatch 진행 중 (docker exec 직후) 서버 강제 종료: `kill -9 $(pgrep -f "bun run src/server.ts")`
       - 서버 재기동: `bun run src/server.ts`
       - startup log에 `[APPLY-05] N unfinished applyPatch detected` 출력 확인
       - 첫 /chat-stream 요청 시 SSE drift event에 a/b/c 명령 안내 표시
       - 결과 02-VERIFICATION.md crash simulation 섹션에 PASS 기록

    5. git commit fail simulation (D-B3):
       - state/.git/hooks/pre-commit을 일시 변경하여 무조건 exit 1 (백업 후)
       - applyPatch 호출 → docker 성공 + git commit 실패 → D-B3 역 docker 자동 시도 envelope 발화
       - 응답 envelope에 `rollback_command`, `rollback_ok` 필드 + UI rolled-back 카드 표시
       - hook 원복

    ### Step C — AUDIT-02 grep
    ```bash
    cd state && git log --since '1h ago' --pretty=fuller --grep "^apply("
    # 출력: 본 verify task에서 만든 commit들이 D-D1 양식대로 표시
    cd state && git log -G "Nonce: " | head -20
    cd state && git log -G "AI-Reasoning: " | head -20
    ```
    각 grep이 본 task에서 만든 commit을 매칭 → 02-VERIFICATION.md AUDIT-02 섹션에 grep 출력 + PASS

    ### Step D — DOG-03 /ship
    1. Phase 2 모든 변경 commit 후 push 가능 상태 확인 (git status clean)
    2. `/ship` 명령 실행 (gstack 라우팅)
    3. PR 생성 매끄러움 확인:
       - PR title이 phase 단위로 의미 있게 생성됨
       - PR body에 Phase 2 changelog (10 plans) 자동 요약
       - 자동화 hooks 통과 또는 사람-요청 시점 명시
    4. 결과 02-VERIFICATION.md DOG-03 섹션에 PR URL + 마찰 메모 기록

    ### Step E — Cleanup
    ```bash
    DOCKER_CONTEXT=default docker compose -f tests/fixtures/test-compose.yml down --volumes
    ```

    ### Step F — git commit
    ```bash
    git add .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md \
            .planning/phases/02-propose-apply-approval-gate/FRICTION.md
    git commit -m "docs(02): 10 — Phase 2 verification + FRICTION (DOG-02/03, AUDIT-02)"
    ```
  </how-to-verify>
  <resume-signal>
    02-VERIFICATION.md 작성 완료 (5 SC + crash + git fail + AUDIT-02 grep + DOG-03 PR URL) + FRICTION.md 작성 완료 + git commit 완료 → "approved"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: 02-VERIFICATION.md 템플릿 작성 + Phase 2 결과 표 채우기</name>
  <files>.planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md</files>
  <action>
    Phase 1 `01-VERIFICATION.md` 형식 차용 (REQ-ID 표 + Success Criteria 표 + Outstanding Items + Phase 3 Entry Gate 또는 Project Wrap).

    템플릿:
    ```markdown
    ---
    phase: 02-propose-apply-approval-gate
    plan: 10
    document: VERIFICATION
    date: <YYYY-MM-DD>
    basis: plans 02-01..02-09 SUMMARY.md
    ---

    # Phase 2 Verification Report

    **Date:** YYYY-MM-DD HH:MM (UTC+9)
    **Phase:** 02-propose-apply-approval-gate
    **Coverage basis:** Plans 02-01 ~ 02-09 SUMMARY.md
    **Test environment:** D-A3 test compose stack on 192.168.0.8 (DOCKER_CONTEXT=default)
    **Operator:** gon

    ## Test Regression — Phase 2

    | Plan | Subsystem | Tests | Pass | Skip | Fail |
    |------|-----------|-------|------|------|------|
    | 02-02 | approval/store | approval/store.test.ts | N | 0 | 0 |
    | 02-03 | state/commit + sanitize | state/commit.test.ts | N | 0 | 0 |
    | 02-04 | docker-context helper | tests/setup/docker-context.test.ts | N | E2E | 0 |
    | 02-05 | proposePatch + system-prompt | tools/proposePatch.test.ts, tools/_index.test.ts | N | 0 | 0 |
    | 02-06 | applyPatch 2PC | tools/applyPatch.test.ts | N | 0 | 0 |
    | 02-07 | sse / loop interrupt+resume | agent/sse.test.ts, agent/loop.test.ts | N | 0 | 0 |
    | 02-08 | server /approval + boot recovery | src/server.test.ts | N | 0 | 0 |
    | **Total Phase 2** | | | **N** | **N** | **0** |

    ## REQ-ID Verification (10 Phase 2 requirements)

    | # | REQ-ID | Status | Evidence |
    |---|--------|--------|----------|
    | 1 | APPLY-01 | PASS | tools/proposePatch.ts + jsdiff createPatch + 6 schema test |
    | 2 | APPLY-02 | PASS | tools/applyPatch.ts 2PC + agent/loop.ts dispatch + UI form |
    | 3 | APPLY-03 | PASS | approval/store.ts (PITFALL #3 prevention) + POST /approval/:id |
    | 4 | APPLY-04 | PASS | applyPatch sequence test (docker→git, fail rollback 모두 검증) |
    | 5 | APPLY-05 | PASS | marker lifecycle test + recoverPendingMarkers + drift event |
    | 6 | APPLY-06 | PASS | state/commit.ts buildMessage + sanitize + spawn -F |
    | 7 | APPLY-07 | PASS | state/.git/hooks/pre-commit + manual rebase 거부 검증 |
    | 8 | APPLY-08 | PASS | src/server.ts model = COPILOT_MODEL_PROPOSE = claude-opus-4-6 |
    | 9 | AUDIT-02 | PASS | git log state/ grep '^apply(' / 'Nonce:' / 'AI-Reasoning:' |
    | 10 | DOG-03 | PASS | /ship 흐름 PR URL: <fill> |

    ## ROADMAP Phase 2 Success Criteria (5 items)

    | # | Criterion | Status | Evidence |
    |---|-----------|--------|----------|
    | 1 | proposePatch unified diff + 5-key 인라인 legend visible | PASS | screenshot/console + UI 시각 6 항목 PASS (02-09 task 2) |
    | 2 | y → docker exec 먼저 → 성공 후 git commit (2PC 순서) | PASS | docker events ts < git log ts (test stack) |
    | 3 | docker fail → git commit 안 됨 + 이전 상태 롤백 | PASS | non-existent-svc 시나리오: outcome=rolled-back, git log 변화 0 |
    | 4 | git log state/ body에 user prompt + AI reasoning | PASS | git log --grep '^apply(' --pretty=fuller 출력 (붙여넣기) |
    | 5 | /ship으로 GitHub PR 생성 | PASS | PR URL: <fill> |

    ## Crash Window Simulation (D-C2)
    - 시나리오: applyPatch docker exec 직후 kill -9 → 서버 재기동
    - 결과: 
      - [x] startup log: `[APPLY-05] 1 unfinished applyPatch detected`
      - [x] 첫 /chat-stream에서 SSE drift event payload에 marker info + a/b/c 명령
      - [x] 자동 복구 안 함 (D-C2)

    ## Git Commit Fail Simulation (D-B3)
    - 시나리오: pre-commit hook 일시 force-fail
    - 결과:
      - [x] applyPatch outcome = rolled-back
      - [x] rollback_command = "compose <역command> <svc>"
      - [x] rollback_ok = true/false (실제 결과 기록)
      - [x] UI rolled-back 카드 표시

    ## AUDIT-02 grep 검증
    ```
    $ cd state && git log --since '1h ago' --pretty=fuller --grep "^apply("
    <붙여넣기>
    ```
    Each commit demonstrates user prompt + AI reasoning recoverable via git log alone.

    ## Outstanding Items (User Action)
    - 본 verification에서 발견된 마찰 / 후속 정리 항목 (있으면)

    ## Time Budget
    | Plan | Reported duration |
    |------|-------------------|
    | 02-01 | <fill> |
    | 02-02 | <fill> |
    | ... |
    | **Sum** | **<fill> h** |

    Total ≤ 8h budget: <PASS/FAIL>

    ## Project Wrap
    - Phase 0 + Phase 1 + Phase 2 = MVP complete
    - Total budget: <Phase 0 + Phase 1 + Phase 2> / 18h
    - Outstanding tech debt:
      - v2 deferred (REQUIREMENTS.md): MULTI-01 / KB-AUTO-01 / NOTIFY-01 / OSS-01 / STATE-SUB-01 / PROVIDER-01 / STORAGE-01 / APPROVAL-MOBILE-01
      - 5-key 'e' UX는 textarea 로컬 편집만 (v2: LLM 재제안)
      - 원격 192.168.0.5 docker-compose.yml 직접 SCP 쓰기 (v2)
    ```

    위 템플릿을 작성하고 Step A~D 결과로 채운 후 git commit.
  </action>
  <verify>
    <automated>wc -l .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md</automated>
  </verify>
  <done>02-VERIFICATION.md min_lines ≥ 100 + 5 SC PASS/FAIL 표 + AUDIT-02 grep 출력 붙여넣기 + crash/git-fail simulation 결과 + DOG-03 PR URL.</done>
</task>

<task type="auto">
  <name>Task 3: FRICTION.md (Phase 2) 작성 — DOG-02 dogfood 마찰 누적</name>
  <files>.planning/phases/02-propose-apply-approval-gate/FRICTION.md</files>
  <action>
    Phase 0/1 FRICTION.md 패턴 그대로. Phase 2 신규 마찰 누적:

    템플릿:
    ```markdown
    # Phase 2 Friction Memo (DOG-02 carry-forward)

    **Phase:** 02-propose-apply-approval-gate
    **Date:** YYYY-MM-DD
    **Carry-over:** Phase 0 FRICTION.md (#1-#10) + Phase 1 FRICTION.md (#11-#20)
    **Scope:** Phase 2 신규 마찰 #21~ (gstack-gsd-melodic-raven 다음 개정판 입력)

    ## #21: <마찰 제목>
    **Where:** plan 02-NN, file/path
    **What:** 무엇이 일어났는지
    **Why:** 원인 추정
    **Mitigation:** 본 phase에서 처리한 우회 또는 향후 개선
    **Severity:** LOW / MEDIUM / HIGH

    ## #22: ...

    ## Phase 2 후보 항목 (실 발견 시 채움)
    - 2PC 순서 docker→git 코드와 ARCHITECTURE.md prose의 git→docker 충돌 — research_artifact_corrections로 해결, 그러나 reviewer/executor 혼동 가능성
    - state/.git/hooks/pre-commit이 state/ 별도 git repo의 .git에 있어 부모 repo로 백업 안 됨 → 02-04 init script가 hook 작성을 idempotent하게 재현해야
    - APPLY-08 텍스트 'Opus 4.7' vs 실제 'opus-4-6' (D-10) — 외부 API 미공개로 lock 변경, 모델명은 .env 한 줄 변경으로 향후 4.7 출시 시 swap
    - LOOP에서 60s SSE chunk watchdog과 2분 approval 대기 충돌 → 30s keep-alive interval로 회피 (advisor lock)
    - 5-key 'e' UX는 v1 단순화 — textarea 로컬 편집만, LLM 재제안은 v2 (UX 학습 후 결정)
    - tools/applyPatch.ts 1.5h 추정이 실제 N분 — happy path + marker + D-B3 + Open Q 3 합산이 빠듯
    - COPILOT_MODEL을 일괄 PROPOSE로 변경 시 비용 영향 (sonnet → opus 6배 영향)
    - gsd-tools.cjs 부재로 plan/SUMMARY commit이 manual `git commit` (Phase 0/1 FRICTION carry)
    - /ship 흐름 마찰 (실 PR 생성 시점에 기록)

    ## Cross-Phase Trends (Phase 0+1+2 종합)
    - LLM 모델명 / API endpoint / SDK 호환성: spike-driven 검증이 정답 (Phase 0 #1)
    - research artifact prose vs code lock 충돌: REQUIREMENTS / ROADMAP을 권위로 (Phase 2 #21 carry)
    - 환경변수 빈 export 함정 (FRICTION #8): startup probe + envelope 메시지로 친절하게
    - in-memory state singleton의 test isolation: _resetForTest 패턴 (Phase 1 LOOP-07 carry)
    - 2PC orchestration: docker exec → git commit 순서 + marker lifecycle + atomic consumed flag = 핵심 invariants

    ## 다음 개정판 입력 항목
    위 마찰 항목들을 `~/.claude/plans/gstack-gsd-melodic-raven.md`의 다음 개정판에 반영:
    - gsd-tools.cjs 명령 정리 (commit 자동화)
    - DESIGN.md 내 prose vs code 모순 검출 자동화 (research_artifact_corrections 패턴)
    - phase plan 작성 시 advisor 호출 시점 lock (orientation 후 substantive 전)
    ```
  </action>
  <verify>
    <automated>wc -l .planning/phases/02-propose-apply-approval-gate/FRICTION.md</automated>
  </verify>
  <done>FRICTION.md 작성 + Phase 2 신규 마찰 5+ 항목 + cross-phase trends + 다음 개정판 입력 항목.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md`
- `test -f .planning/phases/02-propose-apply-approval-gate/FRICTION.md`
- `wc -l .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md` ≥ 100
- `grep -c "PASS\|FAIL" .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md` ≥ 10 (10 REQ + 5 SC + crash + gitfail)
- `grep -c "Criteria" .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md` ≥ 5
- `grep -c "AUDIT-02" .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md` ≥ 1
- `grep -c "DOG-03\|/ship\|PR URL" .planning/phases/02-propose-apply-approval-gate/02-VERIFICATION.md` ≥ 1
- `git log --oneline -1` 출력에 "10 — Phase 2 verification" 포함
</verification>

<success_criteria>
- [ ] AUDIT-02 만족 (`git log state/`만으로 user prompt + AI reasoning 추적 가능 grep 검증)
- [ ] DOG-03 만족 (/ship으로 PR 생성)
- [ ] ROADMAP Phase 2 5 SC 모두 PASS (라이브 또는 mock + manual)
- [ ] crash window simulation 결과 기록 (D-C2)
- [ ] git commit fail simulation 결과 기록 (D-B3)
- [ ] 02-VERIFICATION.md min_lines ≥ 100
- [ ] FRICTION.md (Phase 2) 5+ 마찰 항목 + cross-phase trends
- [ ] PROD 호출 0회 (D-A3 test stack만 사용)
- [ ] git commit 1개 추가
</success_criteria>

<output>
완료 후 manual git commit:
`docs(02): 10 — Phase 2 verification + FRICTION (DOG-02/03, AUDIT-02)`

`.planning/phases/02-propose-apply-approval-gate/02-10-SUMMARY.md` 작성. Phase 2 총 시간 + 18h 예산 잔여 + Project wrap 메모.

Phase 2 출하 = MVP 완성 (Phase 0+1+2). v2 deferred 항목들은 REQUIREMENTS.md에 lock 그대로.
</output>
