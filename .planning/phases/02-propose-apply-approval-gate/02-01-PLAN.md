---
phase: 02-propose-apply-approval-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md
autonomous: false
requirements: []
tags:
  - phase-2
  - pre-execute-gate
  - dogfood
  - operator-action

must_haves:
  truths:
    - "Phase 1 ROADMAP Success Criteria #1/#2/#3/#4 라이브 환경에서 5/5 PASS 증명"
    - "VOYAGE_API_KEY가 회전되어 새 키로 voyage-4-lite 임베딩 호출이 성공"
    - "Phase 2 본격 진입 전 ANTHROPIC_API_KEY 빈 export 함정(FRICTION #8)이 운영자 행동으로 회피됨"
    - "5/5 PASS 결과가 02-01-SMOKE-LOG.md에 명시적으로 기록되고 git commit되어 02-02 이후 plan이 참조 가능"
  artifacts:
    - path: ".planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md"
      provides: "Phase 1 live smoke 5단계 + VOYAGE 키 회전 결과 기록 (5/5 PASS 라인 포함)"
      contains: "PASS"
      min_lines: 40
  key_links:
    - from: "02-01-SMOKE-LOG.md"
      to: ".planning/STATE.md Outstanding operator actions"
      via: "5단계 + VOYAGE 회전 항목 1:1 매핑"
      pattern: "Criteria #"
    - from: "02-01-SMOKE-LOG.md"
      to: ".planning/phases/01-read-only-knowledge-layer/01-VERIFICATION.md"
      via: "PENDING — BLOCKED 라인을 PASS로 갱신"
      pattern: "Success Criteria"
---

<objective>
Phase 2 pre-execute gate (D-E1 lock). Phase 1 코드는 22/22 REQ-IDs unit/integration PASS이지만 ROADMAP Success Criteria #1/#2/#3/#4가 라이브 검증 미완. Phase 2 본격 진입 전에 운영자가 5단계 라이브 smoke + VOYAGE 키 회전을 manual 수행하고 결과를 lock-in한다. **이 plan은 비-구현(non-implementation)이며 신규 코드 0줄을 작성한다.**

Purpose: ROADMAP "Phase 2 Depends on Phase 1"의 strict 해석. Phase 1 라이브 검증 없이 Phase 2 docker exec 흐름을 만들면 PITFALL 폭발 위험. 운영자 조치를 plan 산출물로 wrap하여 02-02 이후가 명시적 증거 위에서 시작.
Output: `.planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md` — 5단계 PASS 결과 + VOYAGE 키 회전 확인.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-propose-apply-approval-gate/02-CONTEXT.md
@.planning/phases/01-read-only-knowledge-layer/01-VERIFICATION.md
@.planning/phases/01-read-only-knowledge-layer/FRICTION.md
@.planning/phases/00-bootstrap-spikes/FRICTION.md

<interfaces>
<!-- STATE.md "Outstanding operator actions" 5단계 (D-E1 lock): -->
<!-- 1) `unset ANTHROPIC_API_KEY && bun run src/server.ts` (FRICTION #8 회피) -->
<!-- 2) http://127.0.0.1:PORT 접속 → "ais-prod redis 어디 쓰여?" → SSE stream + RAG + tool call (Criteria #1) -->
<!-- 3) "지난밤 새벽 1-3시 voice 에러 패턴" → readLogs 호출 + 답변 (Criteria #2) -->
<!-- 4) state/services.yaml에서 stack 1개 임시 삭제 후 reload → drift banner 확인 (Criteria #3) -->
<!-- 5) `sqlite3 data/copilot.db "SELECT name, ok, duration_ms FROM events"` → tool call rows (Criteria #4) -->
<!-- + VOYAGE_API_KEY 회전 (HIGH 보안, Phase 1 carry-forward outstanding) -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Phase 1 live smoke 5단계 manual 수행 + VOYAGE 키 회전</name>
  <files>(수행 시점에 코드 작성 없음)</files>
  <what-built>이 task는 코드를 작성하지 않는다. 운영자가 manual로 다음을 수행한다.</what-built>
  <how-to-verify>
    ### 사전 준비
    - 작업 디렉토리: `/home/gon/projects/gon/gons-works`
    - 호스트: 192.168.0.8 (로컬 개발 환경)
    - 운영 서버 docker context: `home-server` (FRICTION #10 carry-forward)

    ### Step A — VOYAGE_API_KEY 회전 (HIGH 보안)
    1. https://dashboard.voyageai.com/ 접속
    2. 기존 키 revoke + 새 키 발급
    3. `.env`의 `VOYAGE_API_KEY=` 새 값으로 교체
    4. 검증: `bun -e "import('voyageai').then(({VoyageClient}) => new VoyageClient({apiKey: process.env.VOYAGE_API_KEY}).embed({input: ['test'], model: 'voyage-4-lite'}).then(r => console.log('OK', r.data[0].embedding.length)))"`
       → `OK 1024` 출력해야 PASS

    ### Step B — Phase 1 live smoke 5단계
    1. **Criteria 사전:** `unset ANTHROPIC_API_KEY` (FRICTION #8 회피 — 셸의 빈 export가 .env를 덮어쓰지 못하게)
    2. **Criteria #1 (RAG + 라이브 docker):** `bun run src/server.ts` 후 브라우저에서 `http://127.0.0.1:PORT`(포트는 startup 로그) 접속 → 입력창에 "ais-prod redis 어디 쓰여?" 제출
       - 기대: SSE stream으로 답변 출력 + tool-start `listContainers` 또는 `readCompose` 보임 + 답변에 `ais-prod-redis` / `Redis 7` / `port 6385` 같은 services.yaml 메타가 grounded됨
    3. **Criteria #2 (라이브 로그):** 입력창에 "지난밤 새벽 1-3시 voice 에러 패턴" 제출
       - 기대: tool-start `readLogs` 보임 + 답변이 시간 범위/요약 포함 (voice 컨테이너 stop 상태이면 "stopped" 명시 답변도 PASS)
    4. **Criteria #3 (drift):** 다른 터미널에서 `state/services.yaml`의 `stacks:` 아래 한 키(예: `n8n:` 블록 전체)를 임시로 주석처리 → 브라우저 reload → drift banner(`drift` 이벤트) 표시 확인 후 즉시 원복
    5. **Criteria #4 (audit DB):** 다른 터미널에서 `sqlite3 data/copilot.db "SELECT id, name, ok, duration_ms, ts FROM events ORDER BY id DESC LIMIT 10"`
       - 기대: 위 두 NL 쿼리에 대응하는 turn rows + 자식 tool rows (listContainers / readLogs / readCompose 중 일부) 출력

    ### Step C — 결과 기록
    `.planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md` 작성:
    ```markdown
    # Phase 2 Pre-Execute Gate (D-E1) — Smoke Log

    **Date:** YYYY-MM-DD HH:MM (UTC+9)
    **Operator:** gon
    **Basis:** Phase 1 ROADMAP Success Criteria + VOYAGE 키 회전

    ## Voyage 키 회전
    - [x] dashboard에서 새 키 발급 + 기존 revoke
    - [x] .env 갱신
    - [x] embed roundtrip OK (1024 dim) — 출력: OK 1024

    ## Live smoke 5단계
    | # | Criteria | Status | Evidence |
    |---|----------|--------|----------|
    | 0 | unset ANTHROPIC_API_KEY 후 server start | PASS | startup log: "[startup] OK port=XXXX" |
    | 1 | "ais-prod redis 어디 쓰여?" → RAG+docker | PASS | SSE chunk 1초 미만 / tool-start listContainers / 답변에 'redis 7' / 'port 6385' grounded |
    | 2 | "지난밤 새벽 1-3시 voice 에러 패턴" → readLogs | PASS | tool-start readLogs / since,until 인자 / 응답 요약 N줄 |
    | 3 | services.yaml drift → banner | PASS | n8n 키 주석 → reload → drift banner 'unknown=[] stale=[n8n]' / 원복 후 사라짐 |
    | 4 | sqlite3 events SELECT | PASS | 출력 N rows (붙여넣기) |

    ## Conclusion
    - 5/5 PASS — Phase 2 진입 게이트 OPEN (D-E1 satisfied)
    - 후속 plan 02-02 ~ 02-10 진행 가능
    ```

    ### Step D — git commit
    ```bash
    git add .planning/phases/02-propose-apply-approval-gate/02-01-SMOKE-LOG.md
    git commit -m "docs(02): 01 — D-E1 pre-execute gate (Phase 1 live smoke 5/5 PASS + VOYAGE 회전)"
    ```
  </how-to-verify>
  <resume-signal>5/5 PASS 라인 + VOYAGE 회전 OK 라인이 02-01-SMOKE-LOG.md에 기록 + git commit 완료 후 "approved" 또는 "5/5 PASS"</resume-signal>
</task>

</tasks>

<verification>
- 필수 산출물 1개: `02-01-SMOKE-LOG.md`에 5/5 PASS 표 존재
- VOYAGE 키 회전 확인 라인 존재
- git commit 메시지에 "01 — D-E1" 포함
- 어떤 신규 코드도 작성되지 않음 (구현 0줄)
</verification>

<success_criteria>
- [ ] 02-01-SMOKE-LOG.md 존재 + min_lines ≥ 40
- [ ] 5 Criteria 모두 PASS 표기
- [ ] VOYAGE 회전 PASS 표기
- [ ] git commit 1개 추가 (`docs(02): 01 — ...`)
- [ ] 어떤 .ts/.html/.yaml/.json도 수정되지 않음
- [ ] 후속 plan(02-02..02-10)이 이 SMOKE-LOG를 참조 가능
</success_criteria>

<output>
완료 후 `.planning/phases/02-propose-apply-approval-gate/02-01-SUMMARY.md`에 결과 요약 (manual 작성). FRICTION 항목 발견 시 `.planning/phases/02-propose-apply-approval-gate/FRICTION.md` 신설 (없으면 02-10에서 처리).

git commit (manual, gsd-tools.cjs 부재 carry-forward):
`docs(02): 01 — D-E1 pre-execute gate (Phase 1 live smoke 5/5 PASS + VOYAGE 회전)`
</output>
