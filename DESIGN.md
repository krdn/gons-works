<!-- /autoplan restore point: /home/gon/.gstack/projects/krdn-gons-works/main-autoplan-restore-20260505-222837.md -->
# Design: gons-works — AI Operator Copilot for 192.168.0.5

Generated by /office-hours on 2026-05-05
Branch: main
Repo: gons-works (single-user, local)
Status: APPROVED
Mode: Builder (Personal productivity)

## Problem Statement

192.168.0.5 운영서버에 6-10개의 Docker Compose 스택(news-prod / ais-prod / voice / krdn-fx / n8n / open-webui / ai-afterschool-fsd-web 등)이 1인 운영 부담으로 누적되어 있다. 운영 부담의 핵심은 "서비스 상태 보기"가 아니라:

1. **컨텍스트 손실** — `ais-prod redis 메모리가 왜 늘었는지` 같은 질문을 할 때마다 (어떤 서비스가 어떤 redis를 쓰는지, 평소 패턴이 뭔지, 관련 로그가 어디 있는지) 매번 수동으로 떠올려야 함.
2. **상태 변경 추적 부재** — `docker compose up -d`, `restart`, env var 수정 등의 액션이 git/일지 어디에도 안 남고 사라짐. 일주일 뒤 "지난주에 내가 뭘 바꿨더라" 답을 못 함.
3. **반복 명령** — 평일 9-15시만 띄울 서비스, 메모리 alarm 시 재시작 같은 자연스러운 운영 규칙을 매번 다시 짜야 함.

기존 도구(Portainer / Dockge / Beszel / Dozzle / k8sgpt / Robusta)는 (1)을 다루지 않는다 — 일반화된 SaaS이기 때문에 "내 환경의 도메인 컨텍스트"를 알 방법이 없음.

## What Makes This Cool

**AI가 내 운영 환경의 도메인 지식을 알고 있는 operator copilot이고, 모든 액션이 git 커밋이 되어 `git log`가 운영 일지·감사 추적·재현 도구가 동시에 된다.**

대시보드 UI가 아니다. **터미널 같은 대화 인터페이스 + git-versioned `state/` 디렉토리**가 제품이다. "krdn-fx 평일 9-15시만 띄워줘"라고 치면 AI가:
1. compose 파일 읽고
2. systemd timer + docker context 명령 제안
3. unified diff 표시
4. `apply?` 묻기
5. `state/`에 커밋 + 실행

`git log state/`만 봐도 "지난주에 뭘 했나"가 답이 된다. 트윗할 만한 차별화 지점.

## Constraints

- **1인 사용자, 단일 호스트** (192.168.0.5). 멀티유저 / 인증 / SaaS 일반화는 비-목표.
- **주말 16시간 첫 출시 가능**해야 한다. 이번 dogfood가 늦어지면 통합 파이프라인 검증도 늦어짐.
- **기존 자산 활용** — Docker contexts (`dlocal`/`dserver`), Claude API (이미 사용 중), open-webui (:8088), n8n (:5678).
- **Languages**: Bun + TypeScript 우선 (Hono runtime).
- **dogfood 메타-목표**: 이 빌드의 매 phase가 통합 파이프라인 (`/office-hours` → `/autoplan` → `/gsd-new-project` → `/gsd-execute-phase`)을 처음 검증하는 테스트 케이스다.

## Premises

1. **목적은 '내 한 서버용' 도구**다. 일반화 가능 SaaS가 아니다.
2. **차별화 축은 "AI knows my environment + state-as-git"**이다. 'AI가 단추 누르는 대시보드'가 아니다. (Phase 3.5에서 subagent reframe 반영해 강화)
3. **파이프라인 산출물 품질이 독립 목표**다. 대시보드 자체와는 별개로 통합 워크플로우가 잘 돌아가는지 검증해야 한다.

## Cross-Model Perspective

(Codex 인증 만료로 Claude subagent fallback이 cold-read 수행)

**Coolest version they hadn't named:** "대시보드"가 아니라 **로컬 'operator copilot' 터미널 — 추론을 스트리밍하면서 행동하는**. 모든 AI 액션이 `state/`에 git 커밋을 만들어서 `git log`가 audit trail이 되는 **homelab-as-conversation**. 대시보드는 트윗할 만하지 않지만 git-versioned 대화는 트윗할 만함.

**Key insight quote:** *"AI knows my environment's domain context"* — "controls"가 아니라 **"knows"**. 흥분의 핵심은 knowledge layer이지 action layer가 아님. **근육 만들기 전에 기억부터 만들어라.**

**50% 시작점:** **Dozzle** (logs UI) + **Beszel** (single-binary Go metrics agent, MIT)이 50%. 직접 만들 50%는 (a) `services.yaml` + RAG로 환경 도메인 지식, (b) NL→docker tool-use 브릿지 + approve/deny gate, (c) state-as-git 커밋 레이어. k8sgpt/Holmes는 substrate가 달라서 skip.

**주말 16h 제안:** Bun + Hono + SQLite + Claude tool-use + plain HTML/htmx. `server.ts` / `tools.ts` (5개 도구) / `kb/services.yaml` / `kb/index.ts`. 첫 8h read-only ("ask anything, grounded answer"), 다음 8h `proposePatch` write tool. **Skip:** auth, multi-user, FSD 프런트엔드, k8s, metrics 그래프.

## Approaches Considered

### Approach A: Minimal Viable (htmx/Bun) — RECOMMENDED

- **Stack:** Bun 1.x + Hono server, SQLite (event log), Claude API (tool-use), plain HTML + htmx.
- **Files:**
  ```
  gons-works/
  ├── server.ts          # Hono + SSE
  ├── tools.ts           # 5 tools: listContainers/readLogs/readCompose/proposePatch/applyPatch
  ├── kb/
  │   ├── services.yaml  # 손 작업 도메인 지식
  │   └── index.ts       # embeddings → SQLite
  ├── state/             # git-versioned operator state (별도 git submodule 권장)
  ├── public/            # htmx UI
  └── package.json
  ```
- **Effort:** S (16h 주말 1회).
- **Risk:** Low.
- **Pros:** dogfood 파이프라인 짧은 사이클로 한 바퀴 다 돌릴 수 있음 / phase 경계 명확 / 사용자 본인이 즉시 쓸 수 있음.
- **Cons:** htmx는 사용자 평소 stack(React/FSD)과 거리. 통합 파이프라인이 React 프로젝트도 처리 가능한지는 별도 dogfood 필요.
- **Reuses:** Claude API 키, Docker contexts (`dlocal`/`dserver`), 기존 docker-compose 파일들, 192.168.0.5 SSH.

### Approach B: Ideal Architecture (Next.js FSD + Postgres + RAG)

- **Stack:** Next.js 15 (FSD) 프런트엔드, NestJS or FastAPI 백엔드, Postgres 이벤트 스토어, pgvector RAG, OpenWebUI를 LLM provider 임베딩, systemd timer 통합.
- **Effort:** XL (4-8주).
- **Risk:** Medium-High (스코프 폭발 + 동기 부여 burnout).
- **Pros:** FSD 검증, RAG production-grade, 가시성 좋음 / 사용자의 평소 stack과 정렬 / 외부 발표 가능.
- **Cons:** dogfood 우선 목적과 충돌(첫 phase가 너무 큼) / 1인 사용자에게 과한 인프라 / 주말 한 사이클 못 돔.

### Approach C: Lateral — Open-WebUI Pipeline + n8n 안에 살기

- **Stack:** open-webui Tools/Pipelines로 services.yaml RAG + Docker tool-use 함수 5개 등록 / n8n으로 "계획 제안 → git commit → 스케줄 실행" 스테이지 / 별도 UI 없음.
- **Effort:** M (1-2주).
- **Risk:** Medium (open-webui 플러그인 API 변경 의존).
- **Pros:** UI를 안 쓰는 lateral 접근 / 기존 stack 깊게 활용 / 인프라 거의 0.
- **Cons:** open-webui/n8n 의존 vendoring / git audit trail 구현이 stack 위에서 기교 필요 / "내 도구를 만들었다" 느낌이 약함.

## Recommended Approach

**Approach A — Minimal Viable.**

이유: dogfood가 우선 목적이고, 16시간 주말 사이클로 통합 파이프라인을 처음으로 한 바퀴 다 돌려보는 게 가장 가치 큼. B는 나중에 phase를 추가하는 식으로 점진 증설 가능 (event store를 SQLite → Postgres로, htmx → React FSD로). 본질적 가치(AI knows + state-as-git)는 A 안에 이미 다 있음.

**phase 분할 가설** (phase 별 산출물은 `/gsd-new-project --auto @DESIGN.md` 호출로 자동 분해될 예정):

1. **Phase 1 — Read-only operator copilot (8h)**
   - `services.yaml` 수작업 (10개 스택 손으로 묘사)
   - `tools.ts` 3개 read tool: `listContainers` / `readLogs` / `readCompose`
   - `server.ts` Hono SSE + Claude tool-use 루프
   - htmx UI: 한 줄 입력 + 스트리밍 응답
   - 검증: "ais-prod redis OOM 의심되면 어디 봐야 해?" 같은 질문에 services.yaml + 라이브 로그로 답변
2. **Phase 2 — Write tool with approve/deny gate (8h)**
   - `proposePatch` tool: unified diff 생성 → 사용자 y/n
   - `applyPatch` tool: y이면 `state/` 디렉토리에 커밋 + Docker 명령 실행
   - audit trail: 각 액션이 1 commit, message에 user prompt + AI reasoning 요약 포함
   - 검증: "krdn-fx 컨테이너 중지해줘" → diff(stop 명령 + state 변경) → y → commit + 실행

phase 2 이후는 후속 milestone (kb 자동 동기화, embedding 인덱스, 멀티 호스트 등).

## Open Questions

- **kb/services.yaml 손 작업 분량** — 스택 10개 × 평균 5필드(목적/의존성/볼륨/정상 패턴/주요 로그 위치) ≈ 50개 필드. 처음에는 5개 핵심 스택(news/ais/n8n/open-webui/krdn-fx)만 작성하고 나머지는 lazy 추가.
- **state/ 디렉토리 git 저장소 분리 여부** — gons-works 본 repo에 state/ 둘지, 별도 `homelab-state` repo로 빼서 submodule로 둘지. 후자가 audit trail로 더 깔끔하지만 첫 milestone에는 같은 repo로 단순화.
- **LLM provider 선택** — Claude API (`claude-opus-4-7` 또는 `claude-sonnet-4-6`) 직접 vs open-webui 경유. 직접 호출이 간단하지만 open-webui 경유하면 향후 로컬 모델 fallback 옵션. 첫 milestone에는 직접 Claude API.
- **자체 호스팅 vs 192.168.0.5에서 실행** — 코파일럿 자체를 192.168.0.5에서 돌리면 본인 망 외 접근 시 SSH tunnel 필요. 일단 로컬(192.168.0.8)에서 실행하고 dserver 컨텍스트로 원격 제어가 가장 단순.

## Success Criteria

**기능적:**
- [ ] "ais-prod의 redis 컨테이너가 어디 쓰이는지 알려줘" → services.yaml + `docker ps` 결합 답변 (Phase 1)
- [ ] "지난밤 새벽 1-3시 voice 에러 패턴 보여줘" → 라이브 로그 200줄 + 요약 (Phase 1)
- [ ] "krdn-fx 컨테이너 중지하고 메모리 limit 256MB로 줄여줘" → unified diff → y → state/ commit + 실행 (Phase 2)
- [ ] `git log state/` 만으로 지난주 변경 이력 추적 가능 (Phase 2)

**dogfood (메타):**
- [ ] `/office-hours` → `/autoplan` → `/gsd-new-project --auto` 사이클이 이 design doc을 입력으로 받아 `.planning/` 구조를 정상 생성
- [ ] `/gsd-spec-phase 1` → `/gsd-plan-phase 1` → `/gsd-execute-phase 1 --tdd` 가 Phase 1을 8시간 내에 완주하고 `superpowers:test-driven-development` 규율이 실제로 작동
- [ ] `/ship` 으로 GitHub PR 생성까지 매끄럽게 흘러감

## Distribution Plan

- **배포 채널:** 자가 호스팅(systemd service on 192.168.0.8 또는 192.168.0.5). 공개 배포 비-목표.
- **CI/CD:** GitHub Actions로 `bun test` + `bun run build` 자동 검증. main merge 시 192.168.0.5 SSH 배포.
- **외부 OSS 가능성:** Phase 2 이후 services.yaml 추상화·LLM provider 추상화가 일반화되면 별도 repo 분리해서 OSS 가능. 단 1차 목적은 본인용.

## Dependencies

- **차단 요인 없음** — 모든 자산이 이미 갖춰짐 (Claude API key / Docker / SSH / 운영서버 / open-webui 백업 옵션).
- **순서 의존:** Phase 1이 read tool만 다루므로 안전. Phase 2의 `applyPatch`는 운영서버에 영향을 주므로 dry-run 모드 + 명시적 staging environment 사용 권장.

## The Assignment

**이 design doc을 그대로 `/autoplan`의 입력으로 넘겨라.** 즉:

1. 이 파일 (`~/.gstack/projects/krdn-gons-works/gon-main-design-20260505-212527.md`)을 `gons-works/DESIGN.md`로 복사 (또는 심볼릭 링크).
2. `/autoplan` 호출 → CEO/디자인/엔지니어/DX 자동 검토 파이프라인이 DESIGN.md를 보강하고 ARCHITECTURE.md를 만든다.
3. `/gsd-new-project --auto @DESIGN.md` 호출 → `.planning/` 구조 + ROADMAP.md + Phase 1, 2 분해 자동 생성.
4. `/gsd-spec-phase 1` 부터 시작.

이번 dogfood가 통합 파이프라인의 첫 검증이므로, **각 단계에서 어떤 마찰이 있었는지 메모하라** (도구가 실패한 지점, 산출물이 기대와 다른 지점, 사용자가 같은 답을 두 번 친 지점). 그 메모가 `~/.claude/plans/gstack-gsd-melodic-raven.md`의 다음 개정판 입력이 된다.

## What I noticed about how you think

- **"controls"가 아니라 "knows"를 골랐다.** D8에서 subagent의 reframe ("AI 단추 대시보드가 아니라 git-versioned 대화")을 즉시 받아들이고 본인의 D4 답("AI 제어 계층")을 수정한 점. 자기 결정도 더 나은 frame이 보이면 바꿀 수 있는 사람은 흔치 않다.
- **dogfood의 메타-목적을 처음부터 의식했다.** D2에서 "Workflow 적용 대상"을 골랐을 때 *이 repo는 통합 파이프라인의 첫 실제 적용 대상이다*라고 명시했음. 도구를 만들 때 "도구를 검증하기 위한 도구"라는 두 층의 의도를 동시에 갖고 있는 사람은 시스템 사고를 한다.
- **운영 부담을 정직하게 인식했다.** D3에서 가벼운 주제가 아니라 본인 운영서버 6-10개 Docker 스택 부담을 골랐다. 자기 자신을 가장 정확한 사용자로 보는 사람은 최소한 자기 자신은 만족시킨다.
- **'예쁜 UI'를 함정으로 인식했다.** D4에서 "그냥 대시보드를 만들면 Portainer를 설치하는 게 훨씬 빠릅니다"라는 ELI10에 동의하면서 "AI 제어 계층"을 골랐고, 그 후 D8에서 한 단계 더 나아가 UI 자체를 포기했음. 차별화 축이 "더 예쁘게"가 아니라 "구조적으로 다르게"여야 한다는 직관을 가졌다.

## Reviewer Concerns

(office-hours Spec Review Loop는 시간 절약 위해 skip — 아래 GSTACK REVIEW REPORT가 그 자리를 채움.)

---

## GSTACK REVIEW REPORT

Generated by /autoplan on 2026-05-05 (Light mode)
Branch: main | Commit: 333ca9c | Restore: `~/.gstack/projects/krdn-gons-works/main-autoplan-restore-20260505-222837.md`

### Review Mode Decision

**Light autoplan** 선택. 이유: design doc이 (1) office-hours에서 이미 cold-read review 완료, (2) 16h 주말 minimum viable scope, (3) UI scope 없음. CEO phase는 design doc이 strategy를 이미 다룸으로 skip 태그. Design phase는 UI scope 0 keyword hits로 skip. **Eng + DX phase만 본격 실행**.

### Voice Sources

전 phase의 Codex 자리는 글로벌 CLAUDE.md `Codex CLI 사용 금지` 규칙에 따라 **Haiku-2 subagent**로 대체. 본 review는 dual Haiku-subagent voices (cold-read).

| Phase | Voice 1 | Voice 2 | Status |
|-------|---------|---------|--------|
| CEO | n/a | n/a | skipped (design doc reviewed) |
| Design | n/a | n/a | skipped (no UI scope) |
| Eng | Haiku-1 (eng) | Claude-Opus (본체 cross-validate) | ✅ done |
| DX | Haiku-2 (dx) | Claude-Opus (본체 cross-validate) | ✅ done |

### Premise Gate

Premise 3개 모두 재확인 (D2 → A). Review 발견 모두 premise 프레이밍 안에서 phase 1.5 작업으로 자연스럽게 편입.

### Eng + DX Consensus Table

| # | Issue | Severity | Eng | DX | Action Phase |
|---|-------|----------|-----|----|--------------|
| 1 | services.yaml 노쇠 (drift detection) | HIGH | ✓ | ✓ | Phase 1 — staleness marker + lazy auto-init |
| 2 | 에러 메시지 fallthrough (`{problem,cause,fix}`) | CRITICAL | ✓ | ✓ | Phase 1 — try-catch 모든 tool wrapper |
| 3 | applyPatch 원자성 (부분 실패 → git/Docker 불일치) | CRITICAL | ✓ | (의존) | Phase 2 — dry-run + 명시적 rollback |
| 4 | Tool API 명세 부재 (`tools.ts.d.ts` schema) | HIGH | (간략) | ✓ | Phase 1 — 타입 정의 + system prompt에 docs 임베드 |
| 5 | Approval gate y/n/edit/diff/abort 5-옵션 | CRITICAL | (간략) | ✓ | Phase 2 — UI에서 5-키 입력 |
| 6 | Prompt injection via logs (RAG가 비밀 노출) | CRITICAL | ✓ | (의존) | Phase 1 — 로그 패턴 sanitization |
| 7 | Phase 1 시간 폭발 위험 (services.yaml 손작업 2-4h) | HIGH | ✓ | ✓ | Phase 1 — AI가 docker ps 읽고 services.yaml 초안 자동 생성 |

### Single Critical Findings (one voice flagged, still actionable)

| # | Issue | Source | Action |
|---|-------|--------|--------|
| 8 | Claude API rate-limit / SSH timeout 시 SSE leak | Eng | Phase 1 — 30s tool timeout + 60s stream chunk timeout |
| 9 | Git commit message 인젝션 (사용자 prompt 안 escape) | Eng | Phase 2 — newline strip + 길이 제한 |
| 10 | Embedding 모델 미정 (Claude vs OpenAI) | Eng | Phase 1 — Claude Haiku embedding 사용 (비용/지연 최소) |
| 11 | DOCKER_CONTEXT 미설정 시 silent failure | Eng | Phase 1 — 서버 startup에서 명시적 검증 |

### Claude (본체) 추가 발견

| # | Issue | Severity | Action |
|---|-------|----------|--------|
| 12 | Phase 1↔2 LLM 비용 급증 (read-only는 Sonnet 4.6, propose-patch는 Opus 4.7 권장) | MEDIUM | Phase 1 — model 선택을 환경변수로 추출, Phase 2에서 Opus 4.7 default |
| 13 | dogfood 메타-목적과 시간 예산 충돌 — 통합 파이프라인 검증 자체가 늦어질 위험 | MEDIUM | Phase 1 — services.yaml AI 초안 생성을 Phase 0(setup)으로 분리해 Phase 1 시간 보호 |

### Architecture Diagram (Phase 1 + 2)

```
gons-works (192.168.0.8 Bun runtime)
├── server.ts ── Hono SSE ── htmx UI (한 줄 입력)
├── tools.ts ── 5 tools (timeout 30s 각, error envelope { problem, cause, fix })
│   ├── listContainers   ── docker context dserver ps
│   ├── readLogs         ── docker context dserver logs (sanitize SECRET/KEY/PASSWORD)
│   ├── readCompose      ── ssh gon@192.168.0.5 cat <compose-path>
│   ├── proposePatch     ── unified diff into state/ (no apply yet)
│   └── applyPatch       ── 2-phase: git commit state → docker exec → rollback if fail
├── kb/
│   ├── services.yaml    ── 5 핵심 stack (lazy auto-init by AI on first run)
│   ├── stale-check.ts   ── docker ps vs yaml diff, warn header
│   └── index.ts         ── Claude Haiku embeddings → SQLite
├── state/               ── git-versioned (same repo, fast-forward only)
└── public/              ── htmx (1 input, SSE output, y/n/e/d/a keys)

Boundary: 192.168.0.8 (copilot) ──SSH──▶ 192.168.0.5 (managed services)
External: Claude API (model: env-driven, default Sonnet 4.6 / Phase 2 Opus 4.7)
```

### Test Plan (Phase 1 핵심)

- **Unit:** services.yaml parse / RAG top-k retrieval (mock embeddings) / tool argument validation
- **Integration:** Claude API mock (cached responses) / SSH mock (fixed log lines) / SSE frame format 검증 / log sanitization regex (SECRET/KEY/PASSWORD 패턴 제거)
- **E2E (real, but read-only):** "ais-prod redis 어디 쓰여?" → services.yaml + live `docker ps` 결합 답변 검증

### Test Plan (Phase 2 핵심)

- **Unit:** proposePatch diff generation (mock docker state) / commit message format / approval gate state machine (y/n/e/d/a)
- **Integration:** mock applyPatch (log only, no exec) / state/ commit metadata 검증 / rollback path
- **E2E (별도 테스트 stack):** 192.168.0.8 로컬에 test docker-compose 띄우고 "test svc 멈춰줘" → diff → y → state/ commit + 실제 stop 확인. **PROD 절대 금지.**

### Failure Modes Registry

| Mode | Detection | Mitigation |
|------|-----------|------------|
| Claude API 429 | tool loop catch | exponential backoff + UI "rate limited 30s" |
| SSH timeout | 15s tool timeout | error envelope + 사용자 manual SSH 안내 |
| applyPatch 부분 실패 | dry-run 후 actual exec 분리 | rollback script (state/ revert + docker rollback 시도) |
| Git conflict in state/ | pre-commit hook (FF only) | 사용자에게 경고, 커밋 거부 |
| services.yaml drift | startup `docker ps` diff | header warning, "AI에게 yaml 갱신 부탁" 버튼 |
| Prompt injection from logs | regex sanitization layer | RAG 인덱싱 전 SECRET/PASSWORD 라인 제거 |

### Phase Plan Recommendations

원 design doc의 2-phase는 그대로 유지. 단 **Phase 0(setup, 1-2h)** 추가 권장:

- **Phase 0 (1-2h, 신설):** Bun init, Hono SSE 보일러플레이트, Claude API 연동, *AI에게 docker ps 보여주고 services.yaml 초안 받기*. 서버 startup 시 DOCKER_CONTEXT/dserver alias 검증.
- **Phase 1 (8h, 기존):** 3개 read tool + RAG + htmx UI. 핵심 issue #1-2-4-6-7-10-11 모두 phase 1에 반영.
- **Phase 2 (8h, 기존):** proposePatch + applyPatch + approval gate + state/ git. 핵심 issue #3-5-9 phase 2.
- **Phase 2.5 (별도, 선택):** 멀티 호스트, kb 자동 동기화, OSS 분리.

### Cross-Phase Themes

1. **에러 envelope 일관성** — 모든 tool이 `{ problem, cause, fix }` 동일 shape 반환. system prompt에서 LLM에게 "에러는 재시도 말고 사용자 안내" 명시.
2. **services.yaml = 시스템의 기억** — Phase 0 자동 생성 / Phase 1 staleness check / Phase 2.5 자동 동기화. design doc의 "knowledge layer" premise를 코드 구조로 직접 표현.
3. **Phase 0 신설로 dogfood 메타-목적 보호** — Phase 1을 8h 안에 끝내려면 services.yaml 손작업이 사전 처리돼야 함. AI가 만들어주면 dogfood 가치 ↑↑.

### Decisions Made (Auto-decided in Light mode)

| # | Decision | Rationale | Principle |
|---|----------|-----------|-----------|
| 1 | CEO phase skip | design doc이 이미 strategy review 완료 | P3 (pragmatic) |
| 2 | Design phase skip | UI scope 0 keyword hits, htmx 1-input minimum | P3 |
| 3 | Codex → Haiku 대체 (전 phase) | 글로벌 CLAUDE.md 금지 규칙 | (user instruction) |
| 4 | Phase 0 신설 권고 | Phase 1 시간 예산 보호 + dogfood 메타-목적 보호 | P1 (completeness) + P5 (explicit) |
| 5 | LLM model 환경변수 추출 | Phase 1↔2 비용/정확도 trade-off | P5 (explicit) |
| 6 | Approval gate 5-key (y/n/e/d/a) | DX critical, escape hatch 필수 | P1 |

### Status

**APPROVED with Phase 0 addition + 13 review actions**. 다음 단계: `cp DESIGN.md` → `/gsd-new-project --auto @DESIGN.md` → `/gsd-spec-phase 0` (or `1` if Phase 0 작업을 inline으로 처리).


